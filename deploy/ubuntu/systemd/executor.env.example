# Replace with the verified local program image ID. No registry tags or credentials.
NIWA_PROGRAM_IMAGE=REPLACE_WITH_VERIFIED_SHA256_IMAGE_ID
# Optional, only after separate acceptance:
# NIWA_EXECUTOR_OPTIONS="--browser-image sha256:VERIFIED_BROWSER_ID --packages /home/niwa/niwa/runtime/executor/environments/catalog"
