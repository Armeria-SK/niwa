# Niwa

Botごとの人格と独立した記憶を持ち、会話・調査・制作を続けるブラウザアプリ。

**次のセッションは [引き継ぎ資料](docs/HANDOFF.md) から読んでください。**

2026-09-07時点で、保存・内部認可、モデル接続部品、親子タスク、スケジューラー、管理者HTTP APIと承認済みUIを接続しています。日常利用の5機能に加え、公開ページ取得、専用キー設定時のBrave検索、専用実行サービス経由の共有ファイル読み書き、出所付き検索・要約、文脈圧縮、残りの手順の保存と過去手順の再利用、ユーザー指定の定期実行API・管理画面とモデル呼出し総上限、共有会話の変更による起動、関心に応じた活動と休息、サブスク上限時の指定Ollamaへの切替を追加しました。130件のテスト・型チェック・Webを含むビルドが成功し、定期実行の保存・停止・再開・削除をPC／モバイル幅のブラウザーで検証しました。実モデルでの調査完遂、実Brave検索、共有ファイルの管理画面、定期実行の外部通知・費用予算、自発活動の実モデルでの品質確認とUbuntuでの隔離検証・配置は未完了です。詳細は [実装状況](docs/IMPLEMENTATION.md) を参照してください。

モデル設定画面からOllama切替先の取得・保存・解除ができ、設定モデルと直近の応答試行先・理由を分けて表示します。PC／モバイルで検証済みです。

利用上限・接続設定待ちの仕事は、停止状態と期限を尊重しながら1分ごとに接続先を再確認します。133テスト・型チェック・ビルドが成功しています。

共通ルールを設定画面から編集し、全Botの次の応答へ反映できます。版の競合と旧ルールで生成された応答・未実行指示を検査します。135テスト・型チェック・ビルド、PC／モバイル画面検証が成功しています。

自発活動の許可スイッチも保存・実行制御へ接続し、自発仕事とその委任先をまとめて停止・再開できます。通常依頼は継続します。136テストとPC／モバイル画面検証が成功しています。

日次バックアップの実行時刻を日本時間で設定できます。同日の手動保存・再起動・時刻変更による自動保存の重複を防ぎます。137テスト・型チェック・ビルドとPC／モバイル画面検証が成功しています。

リーダーのモデルと、新しく生成するBotの標準モデルを設定画面から選択・保存できます。接続先のカタログと推論強度を検証し、新規Bot設定は既存Botへ影響しません。138テストとPC／モバイル画面検証が成功しています。

成果物画面から共有フォルダーを閲覧し、名前検索・本文プレビュー・ダウンロードができます。保存は8MiB、UTF-8本文表示は64KiBまで。140テスト・型チェック・ビルドとPC／モバイル操作検証が成功しています。上記の共有ファイル管理画面の残件は、この閲覧機能について解消しました。

| 資料 | 用途 |
|---|---|
| [HANDOFF](docs/HANDOFF.md) | 現在地、確定したUI、再開手順、残作業 |
| [PLAN](PLAN.md) | 製品要件・ユーザーの決定事項 |
| [ARCHITECTURE](docs/ARCHITECTURE.md) | 本番ディレクトリ・保存・実行境界の設計 |
| [CODE_REUSE](docs/CODE_REUSE.md) | 上流の固定コミット、移植・参考・除外範囲 |
| [プロトタイプ](prototype/README.md) | 実行方法、実装ファイルと制約 |

現在の `D:\niwa` は、本番では `/home/niwa/niwa/app/source/` に置くソースリポジトリです。製品ルート `/home/niwa/niwa/` には `app/`・`config/`・`state/`・`secrets/`・`workspace/`・`runtime/`・`backups/`・`logs/` を並べます。Linux本番への配置はまだ行っていません。

本体検証はルートで `npm ci` → `npm run check`。Node.js 24.16.0を使用します。
