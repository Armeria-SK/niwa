# Niwa — 次セッションへの引き継ぎ

2026-09-08: BrowserPageを追加。専用contextのnavigate/snapshot/follow、分離worldで最大100要素/20k文字の参照、版・接続状態・href変更の照合、任意clickを実行しないリンク遷移、dialog/download拒否、BrowserRequests経由のFetch応答、中断/終了処理。人工HTMLを実Chromeで表示し、POSTがreaderへ届かないこと・リンク遷移・参照失効を確認。全191テスト成功。worker/PodmanのOS出口・Botのツール接続・承認後の変更操作はまだ未実装。実サーバーPID8800への反映は不要な未接続段階。既存UIのユーザー変更4ファイルは保持。

ブラウザー用取得を追加: public-page.tsのIP検査/DNS固定/各転送先検査/中断/256KB制限を共通化し、readPublicResourceは画像・font・CSS/JS等を破損しないbase64で返す。通常readPublicPageのAcceptとテキスト種別/20k文字表示は維持。任意ヘッダー・Cookie・認証・本文は受け付けない。BrowserRequestsは指定Documentと静的GETだけを許可し、POST/PUT・未知XHR/Fetch・別documentはapproval_requiredとして拒否。64要求/8MBの操作単位予算、転送後document照合、遅い応答の中断を追加。これはworker/OS出口とまだ未接続で、承認後のフォーム実行も未実装。型チェックと全190テスト成功。稼働PID8800は変更なし。次はCdpPipeとBrowserRequestsを専用workerのnavigate/snapshot/参照に基づく操作へ接続し、実ブラウザーで検証する。

ブラウザー着手: Statice指定commitのsrc/browser/cdp.ts・actions.tsとpackage.jsonをGitHub connectorで再確認。ホストの日常ブラウザー探索・固定タブ番号・グローバルCDPは持ち込まず、独立実装のCdpPipeを追加（provenance/statice-browser.json）。専用workerが持つChromium debug pipe、要求ID/target session照合、上限付きフレームとpending、timeout/abort/切断後の不明結果を扱い、勝手に再送しない。3件の人工streamテストと、一時プロファイルのChrome152でBrowser.getVersion→専用context/target/session→空ページ読取→context/browser終了を確認。これはWindows上の人工ブラウザー通信試験で、Podman隔離・外部ブラウザー操作・共通ツール接続の完了ではない。次はworkerの操作と出口制御を実装し、その後に専用Linuxサービスへ接続する。新依存は追加していない。

反映後の読取確認: 実6体全員でmemory_review呼出しを確認し、うち2体に各5件のmemories/audit createdを確認。本文は出力・記録していない。残り4体の選別結果の詳細と自動要約の実モデル結果はまだ未確認。稼働PID8800はf39385cまで、ブラウザー通信の独立モジュールは未接続のため追加再起動は不要。

2026-09-07 f39385cまで稼働反映。ユーザー「サーバー再起動はいつでもしちゃってokです」により、以後は反映に必要な再起動を実行中件数にかかわらず許可。今回直前はrunning/queued0だった。PID30144を照合して停止、backendだけ通常ビルド、同じrootでPID8800をhidden起動。管理APIログイン成功・stderr空。旧回数停止33件が自動再開してrunning6/queued27へ移行（他の仕事はcompleted72/cancelled10/waiting_child5/waiting_provider2）。配信UIは変更していない。
f39385c: 調査ツール・計画・成果物・子仕事・収益仕事のある仕事は、完了候補を保存した後、task_summary_saveで出所付きの結論/理由/未解決/次の手順を保存してから元の返答を一度だけ送る。挨拶だけの仕事には要約を要求しない。現在の仕事自体はまだ変わるので、自動提示の出所から除外。出所本文は短い抜粋にし、元履歴は保持。モデル応答の事実性はモデルの判断を伴い、版と閲覧権限の照合だけで意味上の正しさを保証しない。
184テスト・backend build成功。人工実SQLiteで調査→要約中断→Runtime再作成→要約→完了、読取1回/最終返信1回、要約検索とcompleted表示、不正な要約中ツールの拒否と再開を確認。計画全体は未完了で、次はブラウザー/X/実Ubuntu等の残件を続ける。記憶形成a0db3a5、回数制限削除bccd378/fde3d92も同じ反映に含む。

2026-09-07 ユーザーがgoalを「脱線せずにプランの実装を全て完了に導くこと」へ更新し、計画全体の実装再開を明示した。以前の全体停止はこの再開により解除。外部公開/アカウント作成/OS導入は引き続き個別の許可条件を守り、実装・人工検証は続ける。

記憶形成を追加。会話のある仕事で通常の返答/作業を始める前にmemory_reviewをモデルへ要求し、好み・合意・経験・継続関心を最大5件の出所付き候補として整理する。保存不要は空配列。ツールなしモデルには同じJSONを要求して内部ツール記録に正規化する。別Bot生成等の無関係な出力や不正JSONを公開/実行せず接続待ちの自動再試行へ。モデル呼出しは予定の既存予算へ計上する。
Bot個別DBに本文を持たないtask_memory_reviews完了記録を追加。候補全体の現在会話の出所・実行権を検証してから同DB transactionで保存。自動記憶IDは出所から固定し、訂正/削除監査があれば再抽出で上書き/復活させない。同会話内の同本文も重複抑止。訂正中のモデル応答は破棄して整理し直す。既存会話は次にそのBotがその会話で仕事を始めた時に整理対象になる。一括の実データ変更はしていない。
182テストと検証用backend build成功。人工モデル＋実SQLiteで記憶保存→返答のsystem入力→複数仕事/再起動後の重複抑止、管理者訂正と削除の優先、私的出所/別Bot/停止の拒否、JSON専用応答と不正形式、生成中の訂正を確認。UI変更なし。.local/memory-review-backendへビルド済みで通常dist・PID30144は未変更。実モデルによる候補選別の品質・稼働反映は未確認。次は自動要約と計画残件を続ける。

反映最終確認: bccd378で固定回数停止修正をコミット、追加の重点テストも成功。全178件成功後、訂正4回でも古い回答を破棄して5回目で完了することと同一操作3回後の再検討指示を34件のターンテストで追加確認。実APIは最終running2/waiting_provider1で、PID30144は停止せず、通常dist/backendと配信UIも変更していない。修正は.local/continuation-backendにビルド済みだが稼働環境には未反映。次回は仕事の終了を確認してbackendを通常ビルドし、同じ起動引数/rootで反映する。今回UI変更はないため配信UIの再ビルドは不要。

2026-09-07 限定修正: ユーザー「では修正を進めて」により、1仕事24応答/文脈訂正3回/会話系列16回の固定回数による確認待ちを撤去。期限未指定の新規仕事と明示再試行は無期限相当とし、指定期限と既存仕事に保存された期限は維持する（過去の既定期限と明示期限を推測で区別しない）。旧2種類の回数停止理由に完全一致する未期限切れの仕事だけschedulerが自動再開。全体停止・個別停止・アーカイブ・参加権限・未決承認を尊重し、質問/利用上限/接続待ちを混同しない。
長い入力は古い完全なツール交換、次に古い会話をモデル入力から外す。元の記録は削除せずhistory/task_historyで参照可能。外部操作結果の全文をwork_stateへ毎回重複投入しない。計画更新/履歴参照/同じ操作からの方法変更を共通ルールへ追加し、同じツール引数3回でモデルへ再検討指示を渡すが、人の再開を求める停止にはしない。無限ループを確実に検出する機構ではない。必須指示・最新交換・作業状態だけでもモデル容量を超える場合の確認待ちは残る。新たな要約生成・記憶バックフィルは行っていない。
178テスト・検証先backend build成功。24回超の作業、16回超の通常/分岐会話、既知/未知容量の長い会話、2日後の未指定期限存続、明示期限の失敗、旧制限からの自動再開と停止/承認保護を確認。UI変更なし。現時点では実サーバーPID30144がrunning1/queued1のため反映待ち。実仕事を中断せず、最後に状態を再確認する。既存webのユーザー変更4ファイルを保持。旧全体goalは再開しない。

ユーザー依頼「長くやりとりしているが記憶がなさそう、調査して」に対する読取調査。実state/control.dbと6体のmemory.dbをDatabaseSync readOnlyで確認。会話163件時点、6体ともmemories0・memory_audit0、保持された非discarded task_steps内のmemory_remember呼出し0。deleted_content記録なし。共通ルールにmemory_remember指定/記憶保存指示は見つからない。モデルへツールは全Bot共通で公開、会話にmessage_idを渡す実装は存在するが、BASE_RULESに記憶保存の具体的契機がなく、conversation_sendで発言終了後に自動で記憶を整理する処理もない。モデル任せの保存選択が実際には行われていない。
人工RuntimeでexecuteTurnTool(memory_remember)→memoryPage.total1→次のcontextに1件→Runtime再作成後も1件を確認。保存/API取得/読込/保持の経路はこの検証では正常。実ユーザーの記憶・会話・設定は変更せず、サーバー再起動/コード修正も行っていない。次に修正する場合は記憶形成の契機と重要事項の選別、出所・公開範囲・重複抑止、既存会話からの整理を検討する。今回の依頼は調査であり旧全体goal再開ではない。


ユーザー新規指定（2026-09-07）: 活動を会話処理履歴から分離。できごとは決定事項、仕事はお金に関するもの、承認待ちは明示承認、履歴タブ不要。成果物へ削除、スレッドへホバー時の削除/ピン留め。これは以前停止した全体計画の再開ではなく今回の限定依頼。
c70ec30で対応。decisionだけをできごとへ表示。business_task_registerで収益化・販売・費用等の具体的仕事を明示登録し、仕事タブはその仕事だけ表示（過去の会話返信は自動で収益仕事へ分類していない）。approval_requestは質問と別に内容・版を保存しwaiting_userで停止、管理者が同じ版を承認すると再開、見送りは中止。古い版/二重回答/未認証を拒否し通常resumeで承認を迂回させない。決済や外部公開そのものの実行機能は追加していない。
成果物DELETE、room DELETEを管理APIへ追加。確認Modalで削除範囲を説明。成果物は本体データと関連できごとを消去。スレッドは会話・関連成果物・会話を出所とする記憶を消去し、進行仕事を中止・予定を廃止、古いモデル出力を無効化。参照整合性用のroom/task ID行は本文を空にしてアクセス不可とし、削除台帳を過去バックアップ復元にも適用する。バックアップの通常保持期間は変更しない。共有作業フォルダー内ファイルは別機能であり今回の成果物削除対象外。
スレッドのピン留め/削除はボタンを入れ子にせず独立配置、hover/focus時に表示、タッチでは常時表示。承認済みの配色・文字密度を維持。176テスト成功。人工API/SQLite＋Chrome/Playwrightの1440/390で決定のみ/収益仕事のみ/履歴なし/承認見送り/成果物取消削除/スレッドhoverピン取消削除、別途hasTouchの390でhoverなしピン操作を確認。Browser plugin not available。実データの削除や実承認はしていない。
実行中/queued0を直前確認しPID22244を停止、通常UIをビルドして同じ.local/ui-runtimeをPID30144で起動。実ログインで履歴タブなし・thread controls1・新APIキーを確認、状態17中止109完了25確認待ち3子待ちを維持、stderr空。実businessTasks/approvalsは0で、実モデルの新ツール利用は未確認。前に未反映だったBot削除65b665d/削除後閲覧71f7c59も同じ更新で反映。今回の変更は完了し、旧全体goalは停止を維持する。


7dda794で仕事詳細Modalの保存中操作をガード。同期refで連続送信を拒否し、通信中は追加指示入力と停止/再開/中止/再試行/完了ボタンを無効化、finallyで解除。成功時のみ入力を消し、失敗時は保持。人工API/SQLite＋Playwrightの1440/390で同一イベント内2クリックでもPOST1件・DB指示1件、他操作の無効化、HTTP500後の入力維持と再送成功を確認。現API互換の.local/hint-compatible-webへActivityだけをコピーし通常dist/clientへビルド反映。PID22244は維持、実状態17中止99完了20確認待ち3子待ち5実行中。実仕事に操作なし。Bot削除65b665dと削除後会話71f7c59は引き続きAPI更新待ちで未反映。


反映の直前確認を更新。実APIでrunning/queued=0（cancelled17/completed98/waiting_user19/waiting_child2）を確認し最新backend/UIをビルドしたが、停止直前の再確認で新しい仕事が開始していたためStop-Process前に中断。PID22244は停止せず継続。配信UIは.local/hint-compatible-webから現API互換版へ戻してビルド成功。再確認はcancelled17/completed98/waiting_user19/waiting_child3/running1。Bot削除65b665dと会話閲覧専用71f7c59は引き続き未反映。次回は配信UIビルドを最後の停止直前確認後に行い、一時的なAPI/UI版の不一致を避ける。計画照合ではプログラム実行はProgramLog/共通ターン接続まで実装済み、実Ubuntu/Podman境界・ブラウザー操作・X連携はなお未完了。履歴中のこれらを接続前とする記述は過去時点。


状態連動アイコンを現在の検証用UIで再確認。人工API・実SQLiteの仕事を作成→claim→completeし、待機none、実行sway、完了animationstart1回、その後noneを確認。活動中Botのままページ再読込し、続く2回のstate応答後も完了演出0回・noneを確認した（休眠による演出抑止とは区別）。動きを減らすOS設定、全体停止、明示float設定後の休眠でも静止を確認。Chrome/Playwrightで成功、追加コード修正は不要。実仕事への操作はなし。


Bot削除後の成果物を追加検証。人工API・実SQLite・Chrome/Playwrightで1440/390px両方について、削除済み主体IDによる作成者絞り込み、本文だけに含まれる語での検索、一覧1件、削除したBotの帰属表示、プレビュー本文、ダウンロード実ファイルの本文一致を確認。ブラウザーはテキスト成果物名へ.txtを補う。既存の削除・履歴保持・仕事操作拒否・個別会話閲覧専用の検証も同時成功。コード変更は不要。実環境でBotを削除していないため、これは人工データによる検証であり稼働中への反映完了を意味しない。


削除済みBotとの個別会話は履歴閲覧専用とし、返信・添付・新規セッションを無効化。削除理由の案内をモバイルでも表示し、共有会話の依頼先候補から削除済みBotを除外、選択済みの宛先も送信時に拒否。人工API＋実SQLite＋PlaywrightのPC1440/モバイル390でBot削除、履歴・成果物保持、仕事中止、再試行非表示、個別会話の入力禁止と案内表示、共有の現役Bot候補を確認。ビルド成功。実APIはcancelled17/completed88/waiting_user13/waiting_child2/running5/queued1のため再起動せず、Bot削除機能と今回UIはまだ稼働環境へ未反映。実Botへの削除なし。


ユーザー指定によりREADMEを通常のシステム全体へのNode.jsインストールへ変更。NodeSource 24系を管理者側で導入し、niwaユーザーへの切替とgit cloneを次の手順に分離。要求バージョン24.16.0以上25未満を明記し、私有runtimeパス・PATH設定・xz-utilsを除去。配布元公式setup_24.xを確認、READMEの全17bash枠の構文検査成功。実インストール・アカウント作成・稼働環境変更は行っていない。


ユーザー再指摘によりREADMEのNode導入スクリプトを5402468で再修正。前回はgit cloneとの分離だけだったため、今回は括弧・set/test・一時変数・trapを除去し、mkdir/cd/curl/照合/tar/versionを説明付きの個別枠に分割。npm準備もPATH/cd/npm ci/buildの別枠へ変更。全18bash枠をUbuntu sh -nで構文確認、実インストールは未実施。


65b665dでBot恒久削除機能を確定（13ファイル141追加17削除、うち回帰テストを含む）。admin専用・リーダー除外・表示時profile hash照合、名前入力確認Modal、進行中仕事と委任先の中止／定期実行廃止、人格と本人tool_receipts削除、私的DBディレクトリ消去。削除済み主体は再招集／仕事再試行不可で保存枠は再利用可能。会話・成果物・仕事履歴を保持しUIの作成者を『削除したBot』に表示する。過去バックアップは通常保存期間で保持するが復元CLIは現在のdeleted_agents台帳を適用して復活を防ぐ。ファイル消去途中の残存は起動時にも消去する。173テストと人工APIブラウザーPC1440/390の確認済み（直前記録に詳細）。別UI変更を混ぜずMembersのみ候補内容から選択コミット。現在実APIでcancelled17/completed80/waiting_user6/waiting_child2/running3を確認したため、稼働中PID22244へは未反映。実Botの削除は行っていない。次の反映は仕事終了を確認してAPI/UI同時に実施する。見た目保存の旧画面残存推定はユーザーの再読込後の返答がまだ無いため、解消を実操作で断定していない。


ユーザーから見た目の保存エラー画像、会話内仕事表示の質問、READMEのパッケージ説明とNode/git clone分離を受領。53b6c70でREADMEへgit/curl/ca-certificates/xz-utilsの用途を追加、Node導入・git clone・npm準備を別ステップ化し全10bashブロックをUbuntu sh -nで検査。画像では新しい再読込ボタンがないため旧画面残存によるprofile PATCH契約不一致と推定、ページ再読込を案内。現行配信画面のprofile_versionと再読込ボタンは実ログイン読取で確認（実プロフィールの保存はしていない）。会話の未返信案内と進行中仕事が同時表示される点を修正し、仕事表示へ専用スタイル追加。人工API＋PlaywrightのPC1440/モバイル390で仕事ありの空案内非表示・仕事なしの案内・横はみ出しなしを確認。開発サーバーは稼働のまま、Bot削除の未反映コードを除いた互換UIを.local/hint-compatible-webから通常dist/clientへビルドして反映。PID22244維持、実仕事に操作なし。実API上で実行中／子待ちを確認し、別の仕事は実行区切り到達の確認待ち（モデル生成のバグと断定しない）。

進行中の別作業: Bot恒久削除を未コミット実装中。deleted_agents表、admin削除／仕事中止／定期実行廃止／profileと本人tool_receipts削除／私的DBディレクトリ消去、起動時残存消去、復元時削除台帳適用、保存枠回復、旧主体拒否。API version照合DELETE、名前入力確認Modal、履歴の削除済み表示と再試行非表示まで接続。173テスト・型チェック・検証用ビルド、人工APIブラウザーPC/モバイルで削除／取消／ファイル消去／履歴維持を確認。実Botは削除していない。バックエンドdistはこの未反映変更を含むため次の再起動前に差分と検証を確認する。通常配信UIにはこの機能をまだ入れていない。


プロフィール関連の7b47c2c/e8fa241/4dbaa68を稼働開発環境へ反映。直前の実APIで実行中・待機列0、cancelled17/completed62/waiting_user3を確認。既存PID28556の起動引数を照合して停止し、通常dist/clientへ最新UIをビルド、同じ.local/ui-runtimeで通信可能な環境からPID22244として起動した。管理者ログインとstate全Botのprofile_version64桁、新しい編集再読込ボタン表示を読み取り確認。状態件数は17/62/3を維持、stderr空。実プロフィールやモデルの保存はしていない。確認待ち3件は再開していない。直前までの『未反映』は履歴。171テスト＋人工API/ブラウザー検証済みコードの反映で、実モデル新規応答は未確認。Ubuntu導入・専用主体作成・実Podman試験の許可は引き続き未受領で、今回の開発サーバー更新とは別。


プロフィールとモデルの同時保存を原子的に変更。profile PATCHへ任意selectionを追加し、ModelGatewayは入力を複製してカタログ／能力・接続世代を確認してから、Runtimeでprofileとmodelを同一制御DB transactionで保存する。UIは1回のPATCHだけを送り、モデル確認失敗でプロフィールだけ保存される状態を解消。編集元hashにprovider/model/reasoningも含め、確認中または別画面でのモデル変更も409で拒否。171テスト・型チェック・バックエンド／検証用UIビルド成功。人工HTTP＋実SQLiteで未導入モデル拒否と不変、確認中の別編集拒否、triggerによるモデル書込失敗時のプロフィールrollback、同時成功、外部モデル変更後の古い保存拒否を確認。PlaywrightのPC1440／モバイル390でモデル確認失敗時のデータ不変と入力保持→同じ入力の再保存成功を追加確認。既存profile／appearance競合・再読込・連続保存も成功。Browser plugin not available。通常dist/clientと稼働PID28556は変更せず未反映。直前記録の『プロフィールとモデルが別操作で片方だけ保存され得る』は今回解消。


プロフィール／見た目の編集競合を防止。管理APIのprofile PATCHは{patch,version}必須、stateで名前とプロフィール本文のSHA256をprofile_versionとして返す。制御DB transaction内で編集元と現内容を照合してから既存無効化と更新を行い、不一致409／version欠落400。内部管理呼出しは任意expected、Bot自己編集の権限は従来通り。UIは編集開始時のversionを保持し成功応答のversionへ更新、古い保存で入力を維持、明示『最新の内容を読み込む』で再読込。モデル変更前にprofile競合を確認（プロフィールとモデルの保存は依然別操作で、モデル変更失敗時にはプロフィールだけ保存され得る）。170テスト・型チェック・本体/UIビルド成功。人工実API＋PlaywrightでPC1440/モバイル390のプロフィール／見た目409、入力保持、最新読込、連続保存、横はみ出しなし・pageerrorなしを確認。Browser plugin not available。証拠.local/profile-conflict-qa.cjsと画像。稼働サーバーPID28556は変更せず、通常dist/clientは現サーバー互換の旧App/components/Profileコード＋既存UI変更から再ビルドして維持した。新UIは.local/profile-conflict-clientで検証、未反映。次の反映はAPIとUIを同時に更新する。プロフィール版別履歴やモデル設定の競合制御は別残件。


管理者によるプロフィール変更時、対象Botの保存済みモデル応答・ツール指示を無効化し本文を消去するよう修正。従来は実行中のlease中断だけで、確認待ち等の保存済み指示が再開時に残った。既存memory_state revisionを更新して旧入力も失効させる。制御DB更新より先にmemory DBをtransactionで無効化するため、途中失敗は余分な作り直しになり得るが古い指示を維持しない。Bot自身のprofile_updateの挙動は従来通り。170テスト・型チェック・バックエンドビルド成功。人工モデル＋実SQLiteで変更前agents_create指示の保存→確認待ち→管理者人格変更→Runtime再作成→再開→新人格で完了／Bot非生成を確認。稼働サーバーは再起動しておらず修正は未反映。プロフィールの版別閲覧・編集競合UIは未実装。


Ubuntu実機用verify-program.mjsを追加。--apply明示と空の検証専用workspaceを要求し、実Podmanで共有書き込み／ホスト私的ファイル・リンク先・OS書き込み・外部通信拒否、cgroup設定値、seccomp／NoNewPrivs、非ゼロ終了、出力上限、期限、中断を確認する。人工データのみ生成し自身の生成物を片付ける。deploy/ubuntu/READMEに実行条件と確認範囲・未検証範囲を追記。.gitattributesでshellファイルのLFを固定。node --check成功、--applyなしの起動が実行前に拒否されることを確認。実Podman未実施。これは検証準備の進捗であり、隔離の実証ではない。Ubuntuへのパッケージ／専用主体導入の許可は引き続き未受領。現在稼働中の本体に変更なし。


READMEを外部利用者向けに全面改稿。最新ユーザー指定でUbuntuのみを案内し、Intel/AMD x86_64の同一PCブラウザー利用を対象とした。niwaユーザー作成、公式ハッシュ検証Node24.16.0、clone/npm ci/build、初回設定、起動、管理者キー、実UI名によるモデル接続、終了／再起動、困ったときを掲載。開発経緯・テスト件数・内部引継ぎリンクを外した。プログラム実行・ブラウザー操作・X投稿は利用準備中と明記。README内8コードブロックはUbuntu sh -nで構文検査成功。CLI引数・画面文言をソースと照合したが、Ubuntuへの新規インストール通し試験は未実施。OS導入／アカウント作成の承認は引き続き未受領で、README更新をその許可と扱わない。実環境・稼働サーバーは変更していない。


Ubuntu実機準備の手順を具体化。WSL登録名Ubuntu-22.04の実OSは/etc/os-releaseでUbuntu26.04.1と判明。/home/niwa/niwaは存在し中身は空。Podman5.7.0候補、systemd PID1、標準Node候補22系。apt-get -sでPodman関連の導入候補を確認したが、インストールは未実施。
deploy/ubuntu/prepare-executor.shを追加。明示--apply・root・x86_64・空の製品ルート・未作成の専用主体を要求する。OS依存導入、公式ハッシュ検証Node24.16.0の製品内配置、niwa-exec／niwa-ipc作成、必要ディレクトリのみの所有権・親通過ACL設定を行う準備用コード。サービス起動・認証・イメージ取得・Windowsマウント変更は含まない。WSL sh -n検査成功。実行は未許可・未実施。ユーザー提供AGENTS.mdのdeployment/account operations非許可に従い、OSパッケージと専用主体作成の承認をこれから求める。承認が無い間はこのスクリプトを実行しない。
コードの実行機能は169テスト成功時から変更なし。稼働WindowsPID28556は維持。現在の質問は実機準備への許可であり、Windowsネイティブ対応への方針変更はユーザーから指示されていない。


Ubuntu隔離実行の起動前検査を追加。executorは受付前にPodman infoのrootless／非remote／seccomp／cgroup v2のcpu-memory-pids／HOMEとOSruntime内のコンテナ保存先を検査し、指定sha256イメージがローカルにあることも確認する。未導入イメージを自動取得しない。169テスト・型チェック・バックエンドビルド成功（人工Podman情報の不適合拒否。実Podman試験ではない）。
読み取り専用deploy/ubuntu/check-prerequisites.shと配置・実機受入手順READMEを追加。実Ubuntu-22.04へ標準入力で診断を渡し、Linux・非root niwa(uid1000)・cgroupファイル・Windowsドライブ未マウントを確認。node/podman/niwa-execなしでexit1。Windowsファイルへの/mnt/d経由アクセスは不可だった。パッケージ導入、OSユーザー作成、マウント変更、本番配置は未実施。
ユーザー質問『npmがあればWindowsでも同じ動作か』へ、基本機能は現Windowsで稼働中だが隔離実行／専用ファイルサービスはLinux実装、Windows PCではWSL2が必要と回答。Windowsネイティブ隔離対応を実装した、あるいはnpmだけで全機能が同じとは扱わない。稼働PID28556は変更していない。


プログラム実行の再開を「照合のみ」に制限。Tasks.externalOnceは保存済み操作か新規操作かをサーバー側で区別してcallbackへ渡す。program_runは初回だけallow_start=true、保存済みの実行IDはfalseでサービスへ送る。ProgramLogはfalseの要求で記録が見つからなくても新規実行や新規意図の保存をしない。既存結果は通常通り返す。この指定はモデルのツール引数にはなく、追加しようとすると拒否する。既存の共有ファイル処理は引数追加を無視し従来通り。
168テスト・型チェック・バックエンドビルド成功。実SQLiteから実行記録を消した状態で、共通ツール／実IPC経由の仕事再開が確認待ちを維持し、runner呼出し1回のままであることを確認。未実行の照合が記録を作らないこと、保存済み結果の照合、モデルからのallow_start注入拒否も試験。これは本体の操作記録が残る場合の保護であり、本体・実行サービス両方を過去世代へ戻した場合の保証ではない。
現在のWindows稼働環境にはprogramExecutorUidを追加せず、PID28556も再起動していない。実Podman／Ubuntuでの隔離試験・ディスク上限・本番構成は引き続き残る。


隔離プログラム実行を本体の共通ターンへ接続（設定時のみ有効）。programExecutorUidを明示したLinux構成でprogram.sockの専用主体・パス・権限を毎回検査する。共有ファイルクライアントとendpoint検査を共用。IPCは実行ID付き応答を照合し、1MiB応答・各64KiB出力を上限として不正な形／別実行IDを拒否する。通信の自動再試行はしない。
program_runは接続設定あり・共有会話の場合だけ公開し、モデルからの主体ID・マウント指定は受けない。本体が仕事／会話／Bot／実行IDを付与し、既存externalOnceへ接続。仕事期限・Schedulerの中断をIPCまで伝える。出力はuntrustedとして次のモデル入力へ返す。結果不明は既存のwaiting_userへ移し、同じ実行IDの記録を照合して再開する。個別会話・期限切れ／停止・不正引数は送信前に拒否する。
166テスト・型チェック・バックエンドビルド成功。人工モデル／人工runner＋実SQLite／実named pipeで共通ツール→サービス→結果→仕事完了、非共有拒否、完了済み再送非実行、一時停止の中断伝播と再開時のpending照合を確認。IPCの別ID／巨大応答／不正形／切断・保護確認失敗も試験。稼働Windowsには設定を追加せず、PID28556は再起動していない。実Podman／UbuntuのOS境界、実モデルによるプログラム遂行、専用実行DB紛失・異世代復元の照合、ディスク使用量制限は残件であり、この機能を本番利用可能とはまだ扱わない。


ユーザー指定のBot並列実行を反映。調査ではSchedulerは元々Bot間並列（同じBotの仕事は1件ずつ）、稼働concurrencyLimit=nullだったが、サブスク問い合わせがアカウント共通ConnectionGateで直列だった。CodexConnection.createが仕事ごとに独立したadapter／継続状態を作るため、そのホスト側ラッパーを並列対応に変更。接続・Gateway双方の待ち列を通らず問い合わせを並列送信する。生adapterの既定能力やparallel_tool_callsは変更せず、OAuthAccountの資格情報更新調停・利用上限時の確認間隔・既存の同時実行設定は維持。
163テスト・型チェック・バックエンドビルド成功。人工HTTP＋実adapterで3件同時進行、1件中断と他の継続、別Botの本文非混入、1件時間切れ中の別仕事完了、並列401で認証更新1回を確認。実接続先が複数同時応答を受理する通し試験はまだ未実施。実行・キュー0を確認し同じ.local/ui-runtimeを通信可能な環境でPID25348→28556へ再起動。中止17・完了62・確認待ち3を維持。既存の確認待ち仕事は再開していない。


専用プログラム実行サービスを追加。entrypoints/executorはLinux非root・専用の私的state/home/runtime・共有領域外の保護Unixソケットを要求する。既存のプロセスロックを取得し、保存された未完了コンテナ名だけをPodmanへ回収要求してから受付開始（結果はunknownのまま、再実行しない）。起動時に既存ソケットを削除しない。保護ディレクトリと0600系state・0660ソケットを使用し、HTTPネットワークへ公開する起動経路を設けない。
serverはPOST /programsに限定し、128KiB入力・最大4受付、未知の引数拒否、実行記録による再送処理、通信切断時のAbort、停止時の全実行Abortと完了待ちを実装。内部例外本文は返さない。162テスト・型チェック・バックエンドビルド成功。Windows実named pipe＋実SQLite＋人工runnerで再送結果、スコープ変更拒否、不正形式／上限、切断・停止を検証。Windowsからのexecutor起動は固定エラーとexit1で拒否。
実Ubuntuでの専用主体・Podman・再起動回収・OS隔離は未検証。本体からのIPCクライアント、Bot仕事・停止・結果回収への接続はまだ未実装。サービスをUbuntuに配置／起動したという意味ではない。既存の稼働WebサーバーPID25348は変更していない。


隔離プログラムの永続実行記録ProgramLogを追加。専用DB（共有領域外へ配置する契約）で同期FULLの実行意図を先に保存し、同一実行IDの同時再送・再起動後の再送は開始しない。要求のBot／会話／仕事・コマンド・時間・信頼する実行環境キーをハッシュへ含め、内容変更を拒否する。コマンド配列はawait前に複製する。成功／非ゼロ終了の出力を保存し再送時に返す。中断・出力保存失敗はoutcome_unknownを維持し、自動再送しない。コンテナ名も先に保存し、後の回収処理で同じ対象を識別できるようrunnerへ渡す。
159テスト・型チェック成功。人工runner＋実SQLiteで再起動後の結果再利用、別スコープ／変更要求拒否、2接続の同時配送、入力配列変更、途中中断、実行前／結果保存時のDB失敗、事前キャンセル、閉じたDBを検証。現時点では記録部品であり、実Podman・専用サービス・Botツール・管理画面の確認待ちへの接続、起動時コンテナ回収、失われた実行DB／異世代バックアップの照合は未実装。実行結果不明が画面に表示されるとはまだ扱わない。稼働サーバーPID25348は変更していない。


隔離プログラム実行の最初の部品を追加（src/sandbox/program.ts）。信頼する専用Linux実行主体だけが固定Podman・ローカルsha256イメージを呼ぶ。生成コードをホストシェルへ渡さず、明示したentrypointと引数をコンテナへ渡す。共有/workspaceだけをbind、network=none、非root、capability除去、読取専用OS、一時/tmp、CPU／メモリ／PID／出力／1〜300秒の上限を固定。ホスト環境変数はHOME・XDG_RUNTIME_DIR・PATHだけを選択。中断後は別の期限でコンテナ強制削除を試み、削除失敗は結果不明として失敗する。実行は自動再送しない。
155テスト・型チェック成功。検証は引数境界と人工Podman呼出しによる非ゼロ終了／中断／削除失敗であり、実Podmanの隔離を証明しない。Botツール・HTTP・実行サービスへは未接続。永続実行記録、再起動時の照合、専用サービス、ディスク使用上限、OS隔離の迂回試験が残る。現在の稼働サーバーPID25348は変更していない。
環境の読み取り確認を更新: 制限外でWSL Ubuntu-22.04の存在、非root niwa(uid/gid1000)、cgroup controllersを確認。Ubuntu内のpodman/nodeはcommand -vで見つからなかった。Ubuntu自体が存在しない、または状態取得不能という以前の記述は過去時点。インストール・専用OSユーザー作成・本番配置は行っていない。
仕様参照: https://docs.podman.io/en/latest/markdown/podman-run.1.html （--timeout、--pull=never、--read-only等）。


会話履歴の分割取得を実装・反映済み。最初の発言は固定ヘッダーへ維持し、返信は最新50件から過去へ追加取得する。Appの全会話本文の定期取得を除去し、stateへ件数・最初／最後の日時・最終連番のみ追加。本文は表示中の会話だけ取得し、新着時に更新。未読込の過去本文もサーバー検索の対象。会話切替はkeyで状態を分離し、遅延した古い応答を無視する。古いページ追加時のスクロール位置、ピン順、返信件数を維持。
152テスト・型チェック・本体ビルド成功。人工123返信でページ重複／欠落なし、個別会話認可、%_リテラル検索、カーソル、不正入力、再起動を検証。実API＋PlaywrightのPC1440／モバイル390で分割取得、固定ヘッダー、過去本文検索、新着・大量新着、古いページ取得中の新着、会話切替中の遅延応答、失敗／再試行、横はみ出しなしを確認。Browser plugin not available。
実行・キュー0を確認し、同じ.local/ui-runtimeを通信可能な環境でPID15496→25348へ更新。中止17・完了62・確認待ち3を維持。稼働画面のログイン・会話表示・メンション強調32件／先頭二重0を読み取り確認。人格の版管理・Bot恒久削除、ブラウザー操作・隔離プログラム実行・X連携、本番Ubuntu検証などは引き続き残件。


記憶の取得を表示中のBotだけの50件ページに変更し反映済み。全Botの記憶全文を2秒ごとに取得するApp処理を除去。stateは本文を含まないmemory_version（監査番号）を返し、選択中の記憶画面は作成・訂正・削除の変更時に読み込み済みページを再取得する。作成監査番号のカーソルで続きへ進むため、カーソル対象の記憶が削除されても続きの取得を維持。検索は未読込ページを含む全記憶の文字列検索。Bot切替や検索変更後の古い通信結果を無視する。編集時の表示版を保存APIへ渡し、別画面の訂正を無条件で上書きしない。
151テスト・型チェック・検証用／本体ビルド成功。人工123件の全ページ取得、検索・%_リテラル、境界削除、別Bot拒否、版変化、再起動保持を検証。実APIのPC1440/モバイル390で50→100→123件、未読込ページの検索、古いページ更新、訂正・履歴・削除・新規記憶反映、古い編集の409拒否、通信失敗／再読込、Bot切替中の遅延応答非混入を確認。Browser plugin not available。
実行・キュー0を確認して同じ.local/ui-runtimeを通信可能な環境でPID29176→15496へ再起動。中止17・完了62・確認待ち3を維持。稼働画面も記憶ページ取得1回・全件取得0回・エラーなしを確認（対象Botの実記憶0件、多件数は人工データ検証）。メッセージのページング等は引き続き未実装。

待機中の仕事にも「この仕事を一時停止」を表示（Activity.jsx条件の2行変更）。既存APIを使用し、接続待ち／回答待ち／仲間の結果待ちの3状態に対応。人工DB＋実APIのPlaywrightで3状態×PC1440/モバイル390の停止・再開・中止、停止中の接続自動再試行抑止、子の完了後も親のpausedを維持して実行しないことを確認。検証用・本体ビルド成功、バックエンドは150テスト成功時から変更なし。通常dist/clientへ反映済み、サーバー再起動不要。稼働画面の確認待ち仕事に停止ボタンが出ることを読み取り確認し、実仕事は操作していない。状態は中止17・完了62・確認待ち3を維持。Browser plugin not available。

ユーザーの＠メンション二重表示修正を反映済み（751371f）。Bot本文に宛先がある場合は配送処理が先頭の既存宛先を除いてから1回だけ付与する。複数宛先・全角＠にも対応。既存の先頭の同一二重メンションは保存本文を変更せず表示だけ1つにする。会話中の既知Botメンションと選択済み宛先へ背景色と太字を追加。通常本文はReactの文字列として表示し、HTMLは実行しない。150テスト・型チェック・本体ビルド成功。人工APIのPC1440/モバイル390で背景／太字、二重解消、全角、正規表現記号入り名、メール非誤認、HTML非実行を確認。稼働中の該当会話も読み取り確認し、強調32箇所・先頭二重0・fontWeight700。
実行・キュー0、確認待ち3を確認し、PID20292を停止して同じ.local/ui-runtimeを通信可能な環境でPID29176として再起動。ログインと中止17・完了62・確認待ち3の維持を確認。成果物検索109cf56、仕事履歴42f00e2、一括確認16c7e1aも今回反映済み。
併せて記憶の訂正履歴93d0c46を反映。選択記憶の管理者用corrections APIで訂正者・日時・版の直近100件だけ返す。古い本文を返さず、存在する記憶に限定。管理者権限、別BotのID拒否、上限・再起動保持・削除後404を自動検証。人工APIの画面で訂正保存→履歴、外部訂正時の更新、削除時閉鎖、PC／モバイルを確認。Browser plugin not available。以下の未反映記録は過去時点。実モデルによる修正後の新規投稿はまだ未確認。

できごと一括確認の上限不具合を修正。未確認1003件の1要求が400で全件未確認のままになることを実APIで再現。App.readUpdatesを1000件ずつの逐次送信へ変更（差分3行）。実APIのPlaywrightで1000+3の保存、操作中に新規到着した質問は未確認のまま、再読込保持、個別確認、確認済み表示、390px横はみ出しなしを確認。検証用ビルド成功。バックエンドは149テスト成功時から変更なし。追加のスレッド整理QAでPC1440/モバイル390、ピン／保管の再読込保持、保管中返信禁止、保管から選択して復帰、本文／仕事状態保持を確認。Browser plugin not available。稼働環境は実行3・完了49・中止17を確認、今回もdist/client更新と再起動は未実施。成果物検索109cf56・仕事履歴42f00e2・一括確認修正の反映はアイドル確認後の通常ビルドと通信可能な環境での再起動待ち。

仕事詳細の操作履歴を実データへ接続。GET /api/tasks/:id/historyは既存の仕事・会話閲覧権限を確認し、直近100イベントを時系列で返す。詳細を開いた時と仕事の更新／追加指示件数の変化で再取得し、日時と日本語の状態・操作名を表示。取得失敗は明示。149テスト・型チェック・検証用ビルド成功。人工DB＋実APIのPlaywrightで一時停止・再開・指示・中止・再試行の反映、詳細再表示、PC1440/モバイル390を確認。最初の画面試験で待機中の追加指示がupdated_atを変えないため履歴が更新されないことを発見し、指示件数も更新条件に加えて再検証成功。閲覧範囲、100件上限、順序、再起動保持は自動テスト済み。Browser plugin not available。稼働3210は実行3・キュー1・完了44・中止17、再起動せず。成果物検索109cf56と今回の履歴は通常ビルド・アイドル時再起動待ち。dist/clientは旧版を維持。

状態連動アイコンの追加検証: 人工DBと実APIポーリングを使う.local/motion-qa.cjsで、初期静止→実行中sway→完了animationstartが1回→静止を確認。次の仕事が始まっても過去の完了を再生しない。prefers-reduced-motionでCSS animationName=none、全体停止でnoneを確認。停止解除後に明示floatが動くことを確認してから休眠へ移しnoneとなることも確認した。Browser plugin not available、通常Playwright＋Chrome。コード修正不要。実ユーザーDBは読み取りのみ、最新running3/completed42/cancelled17のため成果物検索109cf56は引き続き稼働画面へ未反映。

成果物本文検索を追加。GET /api/artifacts?query= は名前・説明・本文を検索し、一覧は本文を含まない。既存の会話閲覧範囲を維持、200文字上限・空白・英字大小・%/_のリテラル一致を検証。UIは200ms待機、古い検索結果の無視、検索中／失敗表示、作成者と種類の絞り込みを維持。148テスト・型チェック・検証用ビルド成功。人工DB＋実APIのPlaywrightでPC1440/モバイル390、本文のみの一致、認証、上限、絞り込み、素早い入力変更、通信失敗を確認。Browser plugin not available。稼働3210はrunning3を確認したため再起動せず、既存dist/clientも更新していない。検証用UIは.local/search-client。反映にはアイドル確認後の通常ビルド＋サーバー再起動が必要。

ユーザー指定でモデル応答上限を120秒から600秒へ変更。外側のストリーム監視は605秒。接続の順番待ちを含む600秒で、仕事全体の期限や停止権限は維持。148テスト・型チェック・ビルド成功。3件がwaiting_providerで実行0を確認し、PID2200から20292へ同じ.local/ui-runtimeで再起動。ABORTED誤分類修正も反映済み。対象のモブA/B/Cの3仕事だけ管理者APIで再開し200・running3を確認。600秒経過までの実モデル通し検証は未実施。


ABORTED調査: ユーザー提示19:21のA/B仕事は起動から120.4秒でwaiting_provider、保存済みモデルstep/ツール実行0。Cはweb_read1件実行後に待機。ConnectionGateの120秒タイマーを内部collectModelEventsがABORTEDへ分類する不具合を人工timeout20msで再現。タイマー起因のみTIMED_OUTへ補正し、本来の外部キャンセルはABORTEDを維持。148テスト・型チェック成功。現行サーバーPID2200へは未反映（作業中のため再起動していない）。接続は直列で順番待ちも120秒枠を消費する設計が残る。失敗時の生のイベントは未保存なのでAPI側事情の追加断定はしない。


最新: 共有フォルダーのPNG/JPEG/GIF/WebPプレビューを追加。拡張子でなく取得バイトの署名で形式を選び、既存認証APIの取得内容をimgへ表示する。8MiBの既存上限と元バイトのダウンロードを維持。画像が壊れていればダウンロード案内へ切替。SVG/HTMLは画像として実行しない。ビルド成功、人工ファイル＋実IPC/APIのPlaywrightで4形式×PC1440/モバイル390、元バイト一致、破損画像、偽PNG内HTML非実行、既存の検索・階層移動・上限・領域外拒否を検証。Browser plugin not available。実共有フォルダーの画像は未検証。フロントのみ変更、サーバー再起動不要。


最新: 複数@候補を保持し、1投稿から全宛先の仕事を原子的に作成するよう修正。POST agent_ids対応、旧agent_id互換、宛先順序を変えた再送も重複しない。Botはconversation_sendで相手全員のIDを指定し、サーバーが@表示と配送を行う。通常最終文の先頭に複数@がある場合も互換配送。Botは自分の発言だけをプレーンテキストで書く共通ルールを追加。通常の作業委任の結果待ちは維持。16回の会話上限は分岐全体で数える。
147テスト・型チェック・ビルド成功。人工モデルで複数Bot配送・再送・個別会話権限・分岐上限を検証。PlaywrightでPC/モバイル、2名選択から実APIで2仕事作成を確認（Browser plugin not available）。旧8948をアイドル確認後に再起動、PID2200、3210ログインと活動表示確認。実モデルの新ツール・平文ルールへの追従はまだ未検証。以下の1名限定の記録は旧実装。

Bot宛先会話: 最終発言が「@Bot名」で始まると、同じ会話へ参加できる1名へ次の仕事を配送。リーダーへの代理報告を毎回挟まない。名前が一意に一致しない場合と自分宛ては自動配送しない。通常の作業委任は従来の結果待ちを維持する。Bot側にも@宛先、自分の名前ラベル不要、他Botの台詞を代筆・復唱しない共通プロンプトを追加。自動継続は16回の区切りで次の仕事を確認待ちにし、詳細から再開できる。休息選択、自発活動オフ、全体停止、休眠、アーカイブ、期限、再送、再起動、再試行を既存処理へ接続。

144テスト・型チェック・ビルド成功。人工モデルで宛先配送・リーダー復唱なし・個別会話外拒否・上限後の再開・再起動後の休息を検証。実行／キュー0を確認後、旧19568を終了し、通信可能な環境で同じ.local/ui-runtimeを起動。現PID8948、3210待受・ログイン・活動表示確認済み。実モデルの新プロンプトへの追従はユーザーの次の会話で確認する。以前の認証エラー詳細とBot生成profileも今回の再起動で反映済み。

@宛先指定: 返信欄で@を入力するとBot候補を表示。共有は全メンバー、個別は参加者のみ。候補のクリック／上下キー＋Enterで1名を選び、依頼先表示・解除、@表示を削除したときの解除に対応。POSTのagent_idへ実IDを渡し、再送識別にも宛先を含める。既存APIの参加権限検査を使用。実API＋人工Botで別Botへ仕事が配送され、個別会話に他Botが候補表示されないこと、PC／モバイル、ビルドを検証。複数宛先・手入力だけの名前解釈は未対応。

ユーザー追加要望対応: 会話の日付・操作・最初のメッセージを固定ヘッダーへ分離（長い場合は最大40%内でヘッダー自身をスクロール）。返信のみ独立スクロール。操作列に新規セッションを追加し、同じ公開範囲・相手で新規作成できる。スレッドはピン優先、同区分内は最終発言降順。PC／モバイルで作成・相手／公開範囲保持・新発言時の順序変更・25発言後のヘッダー位置固定・横はみ出し無しを検証。

Bot生成の不足修正: agents_createへ任意のprofileを追加し、役割・性格・形・色・動きを作成時に同じDBトランザクションで保存。roleは表示上の役割のみで生成Botは常にmember権限。モデル・推論強度は管理者の標準設定を使い、ツールから指定不可。旧nameのみの呼び出しと再送記録を維持。141テスト・型チェック・ビルド成功。実モデルが追加profileを正確に渡すことはまだ未検証、現行サーバー再起動未実施。

会話停止の調査: 実会話の直近仕事はcompleted、paused=false/autonomous=true、schedules=[]。Botが文章で別Botへ問いかけたあと新しい仕事を自動作成する経路がなく、会話が継続しない。修正中。既存の設定型shared_changesスケジュールは動くが未登録時の自発参加を担っていない。

会話内の仕事表示: 現在の会話の未完了・待機・失敗仕事を最大3件と総件数で表示し、活動の仕事一覧へ移動できる。中止・完了で表示対象から外れる。実API＋人工仕事による待機理由、他会話の除外、移動、中止後の非表示、PC／モバイルQAとビルド成功。バックエンド141テストから変更なし。既存の吹き出し・メンバー表示の別差分はコミットに含めない。

ユーザー最新指定: 実接続先のカタログにAstraが無いため、リーダーをgpt-5.6-sol / mediumへ変更する指示を受け、3210管理者APIで保存成功。ユーザーが先にLunaへ変更した後の直近仕事3件はcompleted。Solの実応答は変更後まだ未確認。新規生成Botの標準モデルは変更していない。ChatGPTサブスク認証はconnected=true、pending=false、error=nullを確認済み。

認証診断改善: 失敗表示を通信到達不能・交換拒否／時間切れ・受信／状態不一致・保存領域で分け、固定の日本語文言だけ返す。コールバックのCarried表記をNiwaへ修正。141テスト・型チェック・ビルド成功。起動中PID19568にはこの変更はまだ反映していない（ユーザーの接続・会話操作を維持）。

認証接続の起動環境修正: ユーザーのChatGPT認証後にNiwaが失敗表示。制限付き実行環境からauth.openai.com公開設定取得がEACCES、制限外では200を確認。旧PID20900を停止し、通信可能な環境から同じrootを起動（現PID19568、3210待受確認、ログインと活動表示QA成功）。実OAuthの再試行はユーザー待ち。失敗の詳細コードは旧実装で保持していないため、実認証成功はまだ未確認。今後ユーザー用サーバーを制限付き環境へ戻さないこと。

起動中UIの不具合対応: 3210の古いサーバーが/schedulesへ404、/stateにcommonRules無しで最新版Webが初期読込から進めなかった。PID29668の旧Nodeを止め、同じ.local/ui-runtimeを最新版で再起動（PID20900）。管理キーは維持。初期読込失敗のエラー／再試行表示も追加し、実3210へのログイン→人工404表示→再試行→活動表示を検証。ブラウザーの既存セッションは再ログインが必要。

最新: 成果物画面へ共有フォルダーの一覧・階層移動・名前検索・本文プレビュー・ダウンロードを接続。専用ファイルサービス経由の管理者APIを使用。8MiBまで保存、UTF-8の64KiBまで本文表示、一覧200件上限を明示。未接続表示あり。140テスト・型チェック・ビルド、実IPC＋人工共有領域のPC／モバイルQA成功。画像表示・編集・アップロード・削除、Ubuntu隔離実機検証は残件。別の会話・メンバー画面差分は維持。push未実施。

直近コミット: `ccd8844`（Configure verified default models for newly generated bots）。138テスト・型チェック・ビルド、PC／モバイル画面検証成功。別の会話・メンバー画面差分は維持。push未実施。

最新モデル初期設定: 新規Bot用の接続先・モデルID・推論強度をDBへ保存し、生成時に適用する。既存Botは変えない。設定画面の固定されたリーダー／新規Bot欄を、実カタログの取得・検証・保存へ置換。リーダーは既存の個体設定APIを使う。138テスト・型チェック・ビルド、人工接続＋実APIでPC／モバイルの設定保存・再表示・新規適用・既存保持を検証。実アカウントや実モデルは使っていない。全体の概算は6〜7割と回答した（要件基準の目安、テスト数からの算出ではない）。隔離実行／ブラウザー／X／共有ファイルUI／実環境通し検証等は残件。

直近コミット: `8ec072d`（Schedule daily backups at the configured Japan time）。137テスト・型チェック・ビルド、PC／モバイル画面検証成功。別の会話・メンバー画面差分は維持。push未実施。

最新バックアップ時刻: 設定から日本時間のHH:mmを保存でき、日次保存を前回から24時間の方式から指定時刻へ変更。初期値03:00。時刻後に起動して当日未保存なら1回保存し、停止日数分をまとめて再生しない。当日手動保存済みなら自動保存を重ねない。137テスト・型チェック・ビルド、PC／モバイルの設定保存・再表示・実圧縮バックアップを検証。実本番の毎日運用・Ubuntu隔離などは未確認。

直近コミット: `77bc8f1`（Connect autonomy settings to scheduled work and delegated tasks）。136テスト・型チェック・ビルド、PC／モバイル画面検証成功。別の会話・メンバー画面差分は維持。push未実施。

最新自発活動スイッチ: 設定の許可スイッチを管理者API・DBへ接続。オフでは自発予定の新規起動と既存仕事／委任先の実行を止め、通常依頼は継続。オンでは保存済み仕事から再開し、起動回数を増やさない。初期値は従来表示・許可済み方針と同じオンだが、予定は自動作成しない。136テスト・型チェック・ビルド、PC／モバイルのオフ保存・再読込・オン保存を確認。実モデル品質・Ubuntu隔離などは残件。

直近コミット: `65f6927`（Apply editable common rules with revision checks to every bot turn）。135テスト・型チェック・ビルド、PC／モバイル画面検証成功。別の会話・メンバー画面差分は維持。push未実施。

最新共通ルール: 設定の「共通ルール」を管理者API・DB・全Botの共通ターンへ接続。版付き保存で古い画面の上書きを拒否し、空欄で追加指示を解除できる。保存時は進行中の応答を取り直し、保存済みモデル交換にもルール版を持たせて旧版の未実行ツール指示を再生しない。固定の停止・権限・予算・記憶分離制約は別途維持。135テスト・型チェック・ビルド、PC／モバイルの連続保存・再表示・競合・解除を検証。実モデルの追従品質・Ubuntu隔離など全体の残件は未完了。

直近コミット: `08f7652`（Resume provider setup and quota waits through the scheduler）。133テスト・型チェック・ビルド、PC／モバイル設定画面検証成功。別の会話・メンバー画面差分は維持。push未実施。

最新待機再開: 利用上限とモデル接続の設定確認待ちに、1分後の再確認日時を保存。Schedulerが再開し、同じ仕事・保存済み交換を引き継ぐ。通常の通信失敗・回答待ちは自動再開しない。全体停止、仕事停止、休眠、アーカイブ、期限、中止を尊重。133テスト・型チェック・ビルド成功。人工HTTP＋本体Schedulerで、上限待機→再起動→切替先設定→一度だけ完了を確認。移行前の待機には自動再開日時を推測で付けず、手動再開後から適用。実モデル／Ubuntu検証、回復確認が通信失敗した場合の継続運用等は残件。

直近コミット: `c6da90d`（Expose fallback selection and last model connection in settings）。130テスト・型チェック・ビルド、PC／モバイル画面検証成功。残る会話・メンバー画面の差分は維持。push未実施。

最新モデル画面: 設定から導入済みOllamaモデルを取得し、切替先の保存・解除が可能。ツール非対応モデルは保存拒否。直近の応答試行先・モデル・理由・日時をBotごとに永続化し、設定モデルと分けて表示。成功や稼働中を示す記録ではない。130テスト・型チェック・ビルド、人工HTTP＋実API／DBでPC・モバイルの保存・解除・再表示・拒否を検証。待機した仕事の自動再開、実モデル／Ubuntu隔離などは残件。別の会話・メンバー画面差分は維持。

直近コミット: `70a5b77`（Switch quota-limited subscription work to configured Ollama）。130テスト・型チェック・ビルド成功。残る別の会話／メンバー画面差分は維持。push未実施。

最新モデル切替: QUOTA_EXCEEDEDで指定Ollamaへ同じ仕事を継続し、回復確認の成功後は設定済みサブスクへ戻る経路を接続。アカウントIDのハッシュ単位で上限状態を保存し、再起動・他Botへ適用。通常の429／通信エラーでは切替しない。130テスト・型チェック・ビルド成功。実モデル／実アカウントは未使用。現在の接続先・理由の画面表示と、切替先未設定等で待機した仕事の自動再開は残件。

直近コミット: `e1c15ec`（Support interest-led activity and quiet rest within budgets）。127テストと型・ビルド・画面検証済み。別の会話／メンバー画面の作業差分は維持。push未実施。

最新自発活動: 予定に「関心から活動を選ぶ」を追加。共有会話に限り、現在の人格・関心・会話・過去成果を見て、活動またはtask_restで休息を選ぶ共通ターンへ接続。休息は仕事の履歴だけを残し、会話と完了通知は増やさない。通常の依頼では休息ツールを非公開・サーバー拒否。既定は無効で、管理者がBotごとの機会・方針・上限を設定する。127テスト・型チェック・ビルド、PC／モバイル設定QA成功。実モデルの選択品質・金額予算・残る実環境接続は未確認／未実装。

直近コミット: `83d0e27`（Delete schedules while retaining existing work and history）。ユーザー依頼の本体予定削除を保存。前の共有会話変更起動は `a07b993`。残るGit差分は別の会話・メンバー画面変更。push未実施。

最新予定削除: ユーザーの追加依頼により、デモと同じ削除確認を本体へ接続。管理者DELETE /api/schedules/:idと論理削除を実装し、新規起動・一覧表示・再開・古い作成要求による復活を拒否する。予定本文と検知版は消去し、既存仕事・会話・履歴・予算会計の参照は維持。125テスト・型チェック・ビルド、PC／モバイルでキャンセル・確認削除・再読込・仕事履歴保持を確認済み。

最新変更起動: 定期実行にshared_changes条件を追加。登録時の共有会話版から変更があった場合だけ、指定間隔で仕事を作る。自身の発言と個別会話は対象外。追加・訂正・削除を検知し、変化なしではモデルを呼ばない。確認版・次回日時・仕事の保存を同一トランザクションで扱う。API／画面接続、124テスト・型チェック・ビルド、PC／モバイルの選択保存QA成功。関心から活動を選ぶ仕組みと休息は次の残件。これは設定済みの共有会話の変更起動で、全Botの自発活動を完成した宣言ではない。

直近コミット: `68d9f17`（Enforce persistent model call budgets for schedules）。121テスト・型チェック・ビルドとPC／モバイル画面検証済み。残るGit差分は別の会話・メンバー画面変更。push未実施。

最新モデル予算: 定期実行全体のモデル呼出し上限と累計を永続化し、API／画面／共通ターンに接続。モデルへ送る前に予約し、失敗・中断でも消費を戻さない。委任先は親を辿って同じ予算を共有。上限では理由付き待機と「できごと」の通知、再開拒否。121テスト・型チェック・ビルド、PC／モバイルの保存表示QA成功。既存予定は移行時点から数え始め、既定上限は最大起動回数×24。過去の実呼出し数は推定しない。金額・実トークン予算、自発起動は引き続き残件。

最新停止通知: 定期実行が起動上限・連続失敗で停止すると「できごと」に理由と最後の仕事への参照を保存。上限はdecision、連続失敗はquestion。次回予定時刻を待たず、未完了仕事がなくなった時点で判定する。停止と通知は同一トランザクションで、再起動・繰返しtickでも重複しない。118テスト・型チェック・ビルド成功。外部通知や費用総予算、自発起動は引き続き残件。

直近コミット: `ebc91ec`（Connect recurring schedule controls to activity UI）。定期実行画面を検証して保存。残るGit差分は別の会話吹き出し変更2ファイル。push未実施。

最新画面: 活動の定期実行タブから、会話・担当Bot・初回日時・分／時間／日／週間の間隔・起動回数・仕事期限を設定し、保存・停止・再開できる。117テスト・型チェック・ビルド成功。Playwright + Chrome、人工DBとローカルAPIで1920×1080／390×844を検証し、保存後の実DB値、再読込、停止再開、過去日時エラー、個別会話の担当選択、表示とコンソールを確認。実モデルは使わない。通知・費用総予算・自発起動は次の残件。会話吹き出しに関する別の作業差分（Conversation.jsxとstyles.css）はこの定期実行コミットに含めない。

最新定期実行: `6adb64b` にコミット済み、Git差分なし、push未実施。固定間隔・初回UTC時刻・最大起動回数・仕事期限を保存し、管理者APIと既存Schedulerへ接続。117テスト・型チェック・ビルド成功。再起動の重複防止、停止、未完了仕事との重複起動拒否、休眠・アーカイブ・個別会話の認可、保存失敗のロールバックを検証。次は定期実行の画面・通知・費用／モデル総予算、変更検知・関心による自発起動。詳細はIMPLEMENTATIONの最新節。

直近コミット: `0ebb5c6`（Add scoped versioned procedure reuse）。手順再利用の実装・検証を保存し、Gitの作業差分はなし。pushは未実施。

最新手順再利用: 自分の完了済み仕事から、条件・手順・出所版を含む再利用手順を保存できる。検索・読取・新しい版での更新／過去版復元・有効化／無効化・採用を共通ツールへ接続。採用は残作業を設定し、利用した版・理由と実際の仕事結果を参照できる。112テスト・型チェック・ビルド成功。Bot／個別会話の分離と再起動も検証。定期・自発起動、実モデル通し確認等は残件。全体進捗の概算は約60%と説明した（要件単位の目安でありテスト件数の比率ではない）。

最新コミット: ユーザーの「都度コミット」に従い、ここまでの本体UI・実行基盤・検索／圧縮／要約等を `323c8e5`（Implement Niwa app runtime and persistent work continuity）へ保存した。108テスト・型チェック・ビルド成功済み。今後も検証済みの区切りごとにローカルコミットする。pushは未実施。資料・prototype・provenance等は現在の `.gitignore` に従い除外。次の手順再利用は参照箇所の確認だけで、実装はまだ着手していない。

最新要約: `task_summary_save` で結論・理由・未解決事項・次の手順を出所IDと `history_read` の版へ紐付け、Bot個別DBへ保存する。`history_search`／`history_read(kind=summary)` へ接続。108テスト・型チェック・ビルド成功。記憶版変更時は要約と索引を削除し、会話等の出所変更は取得前に検査して古い要約を削除する。バックアップからの削除内容の派生要約・手順の復活防止も検証。自然文はモデルがツールへ渡す補助情報。全仕事の自動要約や、会話入力の要約への置換はまだ行わない。

最新手順保存: `task_plan_update` で現在の仕事の残りの手順をBot個別DBへ保存し、`work_state.remaining_plan` へ毎回戻す。版照合・同じ操作の再送・他Bot拒否に対応。記憶訂正／削除、新しい追加指示・回答で古い手順を削除する。104テスト・型チェック・ビルド成功。DB移行と再起動も検証。手順メモは承認や完了の正本ではない。出典付き要約、過去経験の手順再利用、定期・自発実行は引き続き残件。

最新再参照: `task_history_read` で現在の仕事の保存済み応答とツール結果を再取得できる。Bot・リース・会話・記憶版・応答の有効性を毎回検査し、最大20,000文字の版付きページで返す。元ツールは実行しない。101テスト・型チェック・ビルド成功。再起動をまたぐ取得、訂正・追加指示・破棄後の拒否を検証。残作業の明示保存と出典付き要約は引き続き残件。

最新圧縮: 共通ターンで、容量と出力余裕に応じて完了済みの古いツール交換を入力から外す。会話の40件切捨てを廃止し、会話・現在の依頼・DB作業状態・直近の交換を保持。圧縮時は接続を作り直して再判定する。元履歴と実行上限は変更しない。99テスト・型チェック・ビルド成功。実トークン計測ではなくUTF-8/3+1024の概算。容量不明では128KiBの入力確認上限を超えると待機、既知でも保持必須部分が収まらなければ待機する。会話自体の要約、残作業の明示保存、過去ツール交換の参照は次の残件。

最新容量情報: サブスクのモデル一覧にある `context_window` をプロフィール→アダプター→アカウント直列化部まで渡した。`max_context_window` や要求予算で代用しない。94テスト・型チェック・ビルド成功。これはプロバイダーの提示値であり実測ではない。Ollamaの稼働容量は不明のまま。次は入力サイズの概算、圧縮判定・保持処理の接続と、圧縮不能時の理由付き待機。

最新接続: 共通ターンに `Tasks.workState` を接続。現在の仕事・追加指示・子の結果・決定等の報告・成果物参照・外部操作の結果記録・保存済みモデル応答数を、毎回DBから取得する。Botの成果物作成も仕事へ紐付けた。再起動・旧リース拒否・他Bot拒否・別仕事除外を含む93テスト、型チェック、ビルド成功。作業状態の初期版であり、残作業の明示的な保存、出典付き要約、モデル容量に応じた圧縮は未実装。既存の仕事ID無し成果物は自動推定で紐付けない。

最新部品: Carriedの交換保持処理を `src/runtime/context/retention.ts` へ抽出。初回・最新指示と直近の完全な交換を保持し、ツールID・名前の不一致、重複、結果欠落を拒否する。92テスト・型チェック・ビルド成功。まだ共通ターンには未接続。次は実容量の受け渡し、DB作業状態の再構成と圧縮判定・再開への接続。出典・変更点・ライセンスをprovenanceとTHIRD_PARTY_NOTICESへ保存した。

最新参照: `history_read` を追加し、検索の出所IDから保存元の本文を最大20,000文字ずつ取得できる。続きは返された版とオフセットを必須にし、訂正・削除・範囲外の参照を拒否する。90テスト・型チェック・ビルド成功。次はCarried参照部品を使う文脈圧縮と、NiwaのDBから作業状態を再構成する処理。

最新検索: Bot向け `history_search` に会話・仕事・追加指示・成果物・自分の記憶を接続した。共有情報と現在の個別会話だけを出所ID付きで返す。管理DBのFTS5索引とBot別記憶索引を使い、索引の更新・既存データ移行・訂正削除・短い日本語を検証。89テスト・型チェック・ビルド成功。次は文脈圧縮と作業状態の再構成、出典付き要約。自然文要約・意味検索・画面の横断検索はまだ実装していない。

最新接続: `workspace_write` を共有会話のBotへ接続。本体は操作ID・入力ハッシュ・実行ID・結果を永続化し、実行サービスの重複防止記録を使って再開する。個別会話では共有書込を非公開・拒否。結果不明は確認待ち、停止後の完了は結果だけ保存する。86テスト・型チェック・ビルド成功。実LinuxのOS境界・管理画面からの共有ファイル利用・復元時の実行記録整合は残件。次はPLAN第20節に沿った認可付き過去情報検索と文脈継続、および残る実環境確認へ進む。

最新の書込基盤: 実行サービス側の `WorkspaceWriteLog` に操作IDと入力ハッシュを先に保存し、完了した操作は再送しても再実行しない。途中で中断した場合はファイルのハッシュを照合し、一致しなければ結果不明のまま上書きしない。82テスト・型チェック・ビルド成功。専用サービスの起動引数に共有領域外の私有 `--state` が必須となった。Bot向け書込ツール、本体の書込記録・停止／再開・共有認可、Linux実機の隔離は引き続き未完了。

最新接続: 共有ファイルの一覧・読取を `workspace_list`／`workspace_read` として共通ターンへ接続した。Linuxの製品設定 `workspaceExecutorUid` が明示される場合だけ有効。固定の `runtime/sockets/workspace.sock` に接続し、毎回ソケットの所有者・公開権限を検査する。本体は共有ファイルのパスを直接開かない。79テスト・型チェック・ビルド成功。人工IPCとモデルで読取・応答検証・停止後の破棄を確認した。実Linuxの別主体での運用と、書込の永続実行記録／再開・会話共有認可は次の作業。

最新の作業: 共有ファイルを扱う `Workspace` 部品と、別のLinux実行主体で起動するUnixソケット専用サービス入口を追加した。読取・一覧・版照合付き原子的書込を人工ファイルで検証し、74テスト・型チェック・ビルド成功。本体サービスはまだこの部品を使わず、Botに共有ファイル操作を公開していない。次は実行記録／停止／再開とサービス間接続、共有を許可する会話の範囲を接続する。別OS主体・rootless Podman・Linuxソケット権限・外部プログラムとの競合は未検証。詳細はIMPLEMENTATIONの最新節。

最新追加: `web_search` も共通ツールへ接続。製品ルートの `secrets/brave-search-key` に管理者が設定したキーが存在するときだけ公開し、Brave固定URLへ送信する。キー未設定では検索を公開しない。検索→原文取得→出典付き成果物保存を人工モデル／人工HTTPで確認し、70テスト・型チェック・ビルド成功。実Brave検索・実モデルでの調査は未検証。検索キーの画面設定・共有作業ファイルの読み書きは残る。

直近実装: `web_read` をBotの共通ターンへ追加。公開IPv4の標準HTTP/HTTPSに限定し、DNS結果・転送先を検査して検査済みIPへ接続する。サイズ・時間制限、停止／記憶訂正後の結果破棄、読取結果の再利用を実装。66件のテスト・型チェック・ビルド成功、認証不要のexample.comで実HTTP取得も確認した。HTMLは未信頼のテキストとして返す初期版。検索API・共有ファイル操作・実モデルによる調査完遂はまだ未確認／未実装で、次に接続する。

着手順更新: 最新PLAN第20節・CODE_REUSE第11節を優先する。次は実モデル接続の確認に必要な状態確認と、Web検索・公開ページ取得・共有ファイル読み書き。続いて認可付き全文検索・文脈圧縮・出典付き要約、手順の再利用、定期・自発実行。Carriedの追加参照コミットはCODE_REUSE第11節を正とする。アカウント操作や本番配置の許可をこの計画から推測しない。

直近実装: 指定Ollama切替先の保存・取得・解除と管理者APIを追加。インストール済みのローカルモデルでツール対応を検証し、URL変更時は選択を解除する。61件のテスト・型チェック・ビルド成功。画面接続と実際の自動切替・復帰は残件として保持する。

最新追加: 利用上限を明示するサブスク応答と一般の429を区別し、回復予定時刻を共通モデル契約へ渡す。人工応答を含む60件のテスト・型チェック・ビルド成功。まだ自動切替は行わず接続待ちとなる。次は指定Ollama設定、アカウント共通の上限状態、切替・回復確認・復帰を接続する。詳細はIMPLEMENTATION冒頭。

更新: 2026-09-07。保存・内部認可、モデル接続部品、親子タスク、共通ターン処理、スケジューラー、管理者セッション・HTTP API、排他起動部、Ollama設定を実装。本体用 `web/` に承認済みUIを引き継ぎ、会話・プロフィール・記憶などの保存を接続した。追加された5機能も、できごと・テキスト成果物・仕事操作・スレッド整理・状態連動アイコンとして接続済み。型チェック・人工データによる58件のテスト・Webを含むビルドとPC／モバイル幅のブラウザー試験が成功。環境移行・実モデル応答は未実施。詳細と制約は [IMPLEMENTATION.md](IMPLEMENTATION.md)。

## 1. 現在地と読む順番

1. 本資料で現在地を把握する。
2. [PLAN.md](../PLAN.md) の確定要件を読む。
3. [ARCHITECTURE.md](ARCHITECTURE.md) の本番配置と実行境界を確認する。
4. [CODE_REUSE.md](CODE_REUSE.md) の固定コミットと取り込み範囲を確認する。
5. UIを扱うときは [prototype/AGENTS.md](../prototype/AGENTS.md) と実装を読む。

現状の実ファイルは、ルートの資料・TypeScript設定・`src/`・`tests/` と `prototype/` のReact/Viteアプリ。HTTPサーバー部品は存在するが、本番データ、常駐サービス、Linux配置はまだ存在しない。現在の `D:\niwa` は本番の `app/source/` に相当するソースリポジトリで、8ディレクトリを並べる製品ルートとは別。初版はユーザーの依頼により GitHub `Armeria-SK/niwa` の main に保存済み（`28d902c`）。その後の実装は未コミット。ユーザーの `.gitignore` に従い、資料と `prototype/` はローカルに保持し、強制追加しない。

## 2. 引き継ぐUI

本体ログイン画面への最新指定: 「Niwa」の重複表示と「管理キーを入力して、庭を開きます。」を削除。キーの保存場所の注記だけ最大435pxにし、小画面では左右24pxの余白内へ縮める。入力欄とボタンは既存の最大420pxを維持する。723px／390px幅で確認済み。

ユーザーの最新フィードバックを反映した実装が基準。`prototype/design/` の生成画像と旧QAは初期案の証跡であり、その文字サイズ・カラム比率へ戻さない。

- 会話・メンバー・活動・設定の4画面。会話は検索・共有／個別フィルター付きのスレッド形式。ルーム全体の上部ヘッダーは置かない。
- 1920×1080で本文16px、メッセージのアイコン42px、発言間22px。PCの左右カラムは最新指定の260px／440px。状態名と説明を分け、説明は字下げせず折り返す。メンバー一覧は状態名のみ、会話の「メンバーの様子」は説明付き。小画面では別のレスポンシブ指定がある。末尾の2026-09-07表示修正記録を正とする。
- メンバー・活動・設定の見出しとタブは本文スクロールから分離。設定はフォーム幅820px以内を保ち、スクロール領域をページ右端まで広げる。中央にスクロールバーを戻さない。
- Botの形は20種類。まる／さんかく／ひし形／まめ／くも／しずく／ハート／ほし／つき／カプセル／しかく／みずたま／おばけ／うさぎ／ねこ／くま／きのこ／すず／ピーナツ／ながれぼし。形と任意の色を独立して指定する。
- 素材は `prototype/public/assets/avatar-*.png` のラスターマスク。20ファイルをプロジェクト内に保存済み。花・金属のロボット顔は採用しない。
- ガーデン／サンド／ミスト／ライラック／ミッドナイトの5テーマ。設定→テーマ→設定を保存で全画面へ反映。テーマだけブラウザーの `localStorage`（`niwa-theme`）に保存する。
- スマートフォンは会話一覧と本文を切り替え、下部ナビで4画面を移動する。

## 3. 実装済みと未実装

本体UIは新設した `web/`。ログイン、保存済み会話、投稿、プロフィール・見た目、記憶編集、設定の一部と日常利用の5機能をAPIへ接続済み。58件の内部・HTTPテストと1920×1080／390×844のブラウザー試験を実施した。下表は比較用に残した `prototype/` のデモ状態であり、本体の最新対応範囲と残件はIMPLEMENTATIONの「本体UIの接続」「日常利用の5機能」を参照する。本体でもサブスク認証・実モデルでの初回対話・外部操作・バックアップ等は未完了。

以下はUI接続の状態。バックエンド内部ではBot生成・休眠／再招集、会話保存と閲覧認可、個別記憶の保存・検索・訂正・削除、設定・停止保存を実装済み。画面はまだAPIにつながっていないため、引き続きデモとして動く。詳細は [IMPLEMENTATION.md](IMPLEMENTATION.md)。

| 項目 | 現在の実装 | 本番で残ること |
|---|---|---|
| 会話 | デモのスレッド・返信・検索・個別表示 | DB保存、実際のモデル応答、配信、サーバー側の閲覧認可 |
| メンバー | プロフィール・モデルの選択UI・見た目 | 初回リーダー対話、Bot生成、休眠／再招集、恒久削除、設定適用 |
| 記憶 | 人工データの検索・訂正・削除・訂正履歴UI | 個別DB、認可、実際の検索／文脈反映、削除の復元対策 |
| 活動 | サンプルの進行・承認・履歴・一時停止表示 | 実行キュー、親子依頼・待機・再開、自律活動、実行記録 |
| 設定 | 画面内の編集・保存、テーマ保存 | 認証、設定DB、能力に基づくモデルカタログ、実行境界への適用 |
| 外部操作 | 承認UIだけ。実送信なし | Web・ブラウザ・隔離実行・X・承認内容の固定と重複防止 |
| 添付・成果物 | ファイル名などの表示、固定サンプル資料 | アップロード、実ファイル保管・取得、パス検証 |
| バックアップ | 設定UIだけ | 日次処理、世代管理、整合スナップショット、復元 |

テーマ以外の編集はタブ内の状態だけ。再読み込みで人工データへ戻る。デモの「ナギ・スイ・コハク・ネム」は本番の初期人格ではない。本番はリーダー1体との初回対話から始める。

UI上のモデル名と選択肢は仮データで、実際のモデルID・対応推論強度・認証・並行実行能力を検証した結果ではない。Ollama URLの `localhost` もデモ値であり、WSL2からWindowsへ接続できる設定として確定していない。

## 4. 本番要件の重要な前提

詳細・例外はPLANを正とする。

- Ubuntu／WSL2、製品ルート `/home/niwa/niwa/`。Windowsドライブのマウント無し。Windows側Ollamaへネットワーク接続。RAM32GB、RTX5080／VRAM16GB。
- リーダーはAstra low、生成BotはLuna max。生成はリーダーだけ。生成Botは休眠中も含め10体、リーダー別枠。同時実行数はアプリ側の初期上限なし。管理者による設定変更は可能。
- サブスク上限時は指定Ollamaへ切替、回復後は処理の区切りで元のモデルへ戻る。設定がない場合や利用不能の場合は待機する。
- 記憶はBotごとに独立。共有作業ファイルと共有会話は許可するが、私的記憶と個別チャットを他Botへ自動共有しない。管理者は閲覧・訂正できる。
- Botが作ったコードは別主体のrootless Podman経由。アプリ・管理DB・資格情報・バックアップを渡さない。ネットワーク経由の権限迂回も検証対象。
- Xは共有の専用アカウント。アカウント作成、メール、購入・有料契約、SNS以外のファイル公開、資金提供・実運用開始は管理者の承認対象。
- ブラウザを閉じても本体は活動。手動停止は再起動をまたいで維持。外部操作の結果不明時に無条件再送しない。
- 毎日のローカルバックアップ、初期14日・変更可能。資格情報は含めず再認証。共有成果物は初期の必須バックアップ範囲外。

## 5. 実装順序と次の着手点

サブスク認証・モデル選択も本体へ接続した。設定画面から明示的に認証を開始し、メンバー画面で取得したモデルID・推論強度を選べる。人工OAuth／カタログ／応答と画面操作を検証済み。実アカウント・実モデル・利用枠判定・自動切替は未検証または未実装。古い表の「サブスク認証未実装」はこの追加状況を参照する。

追加実装: バックアップは同期スナップショット→非同期圧縮→保存と世代整理を接続。初回・24時間間隔の自動保存と設定画面の手動保存を人工データで確認済み。新しい製品ルートへの復元コマンド・削除記録の再適用・再認証も人工データで検証済み。管理画面の復元要求、既存ルートへの切替、Linuxの別主体配置は残る。詳細とコマンドはIMPLEMENTATION末尾を参照。

下記1〜3の内部基盤と人工応答試験、5のモデルツール経由の一連の動作、Webセッション認証と排他起動、Ollama設定、4のUI接続の一部を実装・検証済み。実アカウント認証、サブスクモデル選択・フォールバックの統合、OS隔離は未実装。CLIは保存されたOllama設定を使用し、未設定・サブスクの仕事は接続待ちになる。

1. 作業対象と実環境を確認し、現在のUI・アセット・資料を保った状態で本番用の土台を用意する。WSL2の実機状態は未検証なので決めつけない。
2. TypeScriptの契約・管理DB・個別記憶DB・主体IDに基づく認可を実装する。人工データで越境を拒否するテストを先に作る。
3. 固定コミットから必要な接続部品を移し、モック試験後に認証・モデル一覧・1体の応答を検証する。Carriedの `supports_parallel_sessions: false` を未検証で変更しない。
4. 承認済みUIの状態操作をAPI・イベント配信へ接続する。Reactの画面・CSS・20画像・5テーマを引き継ぎ、旧Statice UIへ置き換えない。
5. リーダー1体→子1体生成→依頼→子の完了→親の再開→休眠→同じ記憶で再招集、という最初の一連の動作を通す。
6. 隔離環境、永続停止・再起動復旧、フォールバック、バックアップと復元、承認付き外部操作へ広げる。外部操作・サービスの実稼働はその時点の依頼範囲に従う。

実装時に残る設計課題: 共有ルームとUIスレッドのDB上の対応、Bot間個別会話の参加範囲、初回セットアップ・恒久削除の画面、モデルカタログ／回復判定、共通プロンプト最終文面、X接続方式、具体的な自律起動間隔。PLAN第17節と合わせて解消する。これらをUI承認済みという理由で実装済み扱いしない。

## 6. ローカルでの再開と確認

本体検証は `D:\niwa` で `npm ci` → `npm run check`。Node.js 24.16.0で検証済み。WSL一覧はアクセス拒否で、Ubuntuの状態は未確認。

UIデモの作業ディレクトリは `D:\niwa\prototype`。`package-lock.json` を使い、依存がなければ `npm ci`。起動は次のコマンド。

```powershell
npm run dev -- --host 127.0.0.1 --port 4173 --strictPort
```

既に4173でNiwaが起動していればそのプロセスを利用する。前セッションの端末IDやタブIDは再利用を前提にしない。プレビューURLは `http://127.0.0.1:4173/`、`#conversation`／`#members`／`#activity`／`#settings`。

- `npm run build`: 最終のテーマ・アイコン・スクロール修正後に成功済み。
- ブラウザーで1920×1080、1366×768、390×844の関連画面を確認済み。設定の右端スクロール、ヘッダー固定、20形の表示と追加形・任意色の保存、5テーマ切替、テーマの再読み込み保持を確認。最終コンソールのerror/warnは0件。
- 実スマートフォン、ソフトウェアキーボード、スクリーンリーダー、実認証・実Bot・実外部操作・Linux隔離は未検証。
- `npm run test:sites` はSitesへの引き渡し用。本番ランタイムのテストではない。今回は公開しておらず、このテストを通したとの記録もない。
- `.openai/hosting.json`、`worker/`、`scripts/prepare-sites-build.mjs`、`tests/sites-worker.test.mjs` はテンプレート由来の公開用部品。Niwaのバックエンドではない。公開依頼もない。

## 7. ソースの入口

| ファイル | 内容 |
|---|---|
| `prototype/src/App.jsx` | 画面切替、デモ状態、テーマ保存 |
| `prototype/src/data.js` | 人工データ、SHAPES、THEMES |
| `prototype/src/components.jsx` | Avatar、見た目編集、Modal等 |
| `prototype/src/pages/` | Conversation、Members、Activity、Settings |
| `prototype/src/styles.css` | 基本・レスポンシブ・密度・スクロール |
| `prototype/src/themes.css` | テーマ別トークンと配色 |
| `prototype/public/assets/` | 20種類の画像マスク |
| `prototype/design/avatar-expansion.md` | 追加15画像の制作プロンプトと保存先 |

CSSのレイヤーでは `!important` の優先順が通常と逆になる。既存のメッセージアイコン42pxはcomponentsレイヤーの指定が優先するため、後ろにresponsiveのサイズを書き足すだけでは変更されない場合がある。

## 8. 次回の依頼文例

> D:\niwa の docs/HANDOFF.md と関連資料を読んで再開してください。現在のUIは承認済みです。実装済みと未実装を区別し、UIを維持しながらNiwa本体の実装を進めてください。

これはユーザーが次回使える例文であり、今回の資料確認中に本体実装を始める指示ではない。


## UI追記: メンバー追加

メンバー画面の「メンバー追加」から名前・役割・性格・20種類の形・色を指定できる。管理者からリーダーへの追加依頼を表すUIで、プロトタイプでは即時にデモメンバーへ追加する。生成数上限（リーダー除外・休眠含む）、同名・空名を検査し、標準生成モデルを適用。追加後は詳細へ移り、共有スレッドの参加者に含める。新しい記憶は空。実際のリーダー生成・API接続は未接続。

## UI追記: 配色プレビューと動くアイコン
テーマは選択直後に全画面プレビュー。保存でブラウザーに保持し、変更を戻す・設定画面を離れると未保存分を破棄。メンバーの見た目と追加フォームに動き（なし・ふわふわ・ゆらゆら・ぽよぽよ）を追加し、保存後は各画面のBotアイコンに反映。既存画像マスクをCSSで動かし、端末の動きを減らす設定を尊重。動きは他のメンバーデモ状態と同様に再読込で初期化。

## UI追記: 日常利用の5機能
- ルートURLは活動のできごとを表示。活動に「できごと・仕事・承認待ち・履歴・成果物」を配置し、メイン4画面を維持。できごとは完成・決定・質問から元の会話／仕事／資料へ移動し、個別・一括確認済みを保持。新しい完了・承認判断も反映。要約生成や実イベント取得は未接続。
- 成果物一覧はサンプル2資料を種類・作成者・検索で絞り込み、対応する本文プレビュー・ダウンロード・関連会話へ移動できる。実成果物の収集は未接続。
- 仕事の詳細に一時停止・再開・中止・再試行・手動完了・追加指示・操作履歴・待機理由を追加。個別停止と全体停止を分離し、失敗サンプルで再試行を確認できる。操作はデモ内の状態だけを更新する。
- 会話上部にピン留めとアーカイブ、一覧に保管フィルタ。ピン優先ソート、保管中の返信無効化と復帰、再読み込み保持。会話そのものは削除しない。
- アイコンの「状態に合わせる」を標準に追加。進行中の仕事で揺れ、完了時に一度弾み、休眠・全体停止で静止。端末の動きを減らす設定を尊重。手動の動き選択も維持。
- テーマ・ピン／保管・確認済みIDだけをlocalStorageに保存。他のメンバー／仕事デモ状態は再読み込みで初期化。本体API接続、認可、実モデル処理は今回の範囲に含めていない。
- 検証: 最終ビルド成功。1920×1080・1366×768・390×844で、確認済み保持、完了報告、資料検索／絞込／ダウンロード、仕事の全操作、ピン／保管／復帰／再読込、休眠／全体停止／動きを減らす設定を確認。ダークテーマと固定見出し、1366pxでのサイドバー下部表示も確認。ブラウザーのアプリerror/warnは0件。

## 計画追記: 実用能力とCarriedの文脈圧縮（2026-09-07）

ユーザー依頼によりPLAN第20節に、外部ツール→記憶検索と長い仕事の継続→手順再利用→定期・自発実行の順序と受け入れ条件を追加した。Carriedのmainと会話対応ブランチを調査し、CODE_REUSE第11節に固定コミット・元ファイル・流用判断を記録した。圧縮の部品は抽出可能だが、自然文要約と過去仕事検索、NiwaのBot/会話別認可・訂正削除は別途実装する。今回は文書更新とソース・テスト定義の確認のみ。実装・テスト実行・実接続・配置は行っていない。

## 表示修正: 長い状態文の見切れ（2026-09-07）

web/src/styles.css と prototype/src/styles.css の status-label を1行固定から枠内の折り返しへ変更。max-width/min-width、overflow-wrap、行間とドットのベースライン配置を指定した。メンバー一覧、会話の右側、スマートフォンのメンバーの様子に適用。既存の小画面メンバー一覧で状態文を非表示にする仕様は維持。
本体npm run build成功。Browser plugin not availableのためPlaywrightで127.0.0.1:4173のプロトタイプと本体CSSを差し替えた同一DOMを検証。1920/1366/390pxで長い日本語と連続英数字の折り返し・横はみ出しなし、メンバー切替、モバイルの様子ダイアログ、画面内容・URL/title・Vite overlayなし・console errorなしを確認した。本体API経由の実データ表示と実端末は今回未検証。人工文をDOMに注入して長文を再現し、本番データは変更していない。

2026-09-07 状態文の追加指定: 共通StatusLabelで最初の「 · 」を区切りとしてステータス名と説明を分離し、「再試行待ち：」の次行に説明を0.5em字下げして表示する。説明なしの休眠・停止等は1行。web/prototype両方のcomponents.jsxとstyles.cssへ適用。本体ビルド成功、Playwrightで1920/1366/390pxのプロトタイプを確認（メンバー切替、右側・モバイルダイアログ、改行・全文・横はみ出しなし、pageerrorなし）。本体API経由の表示は未検証。
2026-09-07 追加調整: ユーザー指定により状態説明の0.5em字下げを撤去し、ステータス名と左端を揃えた。デスクトップの右側「メンバーの様子」を240pxから300pxへ拡張。web/prototype両方のCSSへ適用。本体ビルド成功。Playwrightで1920/1366/390pxのメンバー切替・様子表示・横はみ出しなし、説明の左端一致とPC欄幅300pxを確認。
2026-09-07 最新幅指定: メンバーの様子を300pxから440pxへ拡張（web/prototype）。ナギの再試行待ち説明がPC1920/1366pxで1行になることをPlaywrightで確認。390pxのダイアログと横はみ出しなしも確認、本体ビルド成功。4173デモを非表示のバックグラウンドViteとして再起動。
2026-09-07 メンバー一覧の追加指定: 状態の説明文は不要。StatusLabelのshowDescriptionを一覧だけfalseにし、コロンも外して状態名のみ表示。会話のメンバーの様子は説明付きのまま。web/prototypeに反映。本体ビルドと1920/1366/390pxでメンバー切替・状態名のみ・会話説明維持のPlaywright確認が成功。

2026-09-07 吹き出し表示: ユーザー提示の参考画像をもとに会話本文・引用・添付をmessage-bubbleへまとめ、名前・時刻・スレッド題名は外側に配置。淡いテーマsurface色、丸角、左下だけ小さな角、本文幅に合わせたサイズにし、アイコンを左下へ配置。旧返信の縦線は撤去。web/prototypeのsrc/pages/Conversation.jsxとsrc/styles.cssへ反映。本体ビルド成功。Browser plugin not availableのためPlaywrightでデモの1920/1366/390px、送信・引用返信・改行長文・長いURL・横はみ出しなし・pageerror/console errorなしを確認。5テーマの背景切替も確認。本体API・添付ファイル操作の再試験は未実施。

## 2026-09-07: 上下スクロール矢印と本体機能のデモ追加

ユーザーのスクロールバー表示指定: メンバー一覧は棒とトラックを隠し、はみ出すときだけ上下のボタンを表示。端に到達した方向は無効化。MemberScroll.jsxでResizeObserver・scrollイベントに追従し、ホイール・キーボード・タッチはネイティブスクロールを維持。小画面の横並び一覧は既存動作を維持。web/prototype両方のsrc/MemberScroll.jsx（新規）、src/pages/Members.jsx、src/styles.cssに適用。ネイティブ疑似要素の矢印案は動作確認できなかったため撤去し、実ボタン方式に置換済み。

ユーザーの「本体web/srcにありデモにない機能を追加」依頼に対し、次をprototypeだけへ追加:
- 活動→定期実行: 本体のSchedules.jsx/schedules.cssをベースに、3件のサンプル（有効・停止・回数到達）、予定追加、会話/担当Bot選択、初回日時・間隔・回数・期限、停止/再開、全体停止/休眠/会話アーカイブ時の待機理由を表示。
- 設定→バックアップ: BackupStatus.jsx、人工履歴と「今すぐバックアップ」。実ファイルは作らない。
- 設定→モデルと接続: SubscriptionConnection.jsx、未接続/接続待ち/中止/接続完了/解除のデモ。外部ログインリンクは作らず、明示的な「デモの接続を完了」操作に置換。
- メンバー→プロフィール: 本体のモデルカタログ操作を抽出し、Codex/Ollamaのサンプル一覧、接続前エラー、モデル/推論設定の選択を追加。
- 設定→モデルと接続→本体のログイン画面を確認: Login.jsxをデモ化。キーdemo-niwaで成功、他の入力はエラー。実資格情報の入力は不要。フォームの入れ子を避けるためPortalとイベント伝播停止を使用。

追加/変更ファイル（prototype/src）: demo-api.js、BackupStatus.jsx、SubscriptionConnection.jsx、Login.jsx、pages/Schedules.jsx、pages/schedules.css（以上新規）、App.jsx、pages/Activity.jsx、pages/Settings.jsx、pages/Members.jsx、styles.css。既存の長文18発言デモ、吹き出し、配色・アイコンを維持。
demo-api.jsはfetchを持たないローカル専用状態。新しい予定・認証状態・モデル変更・バックアップ履歴はページ再読込で初期化。予定時刻に自動起動せず、モデル・アカウント・保存先・外部サービスには接続しない。認証やDBの内部処理はデモへ移植していない。本体の追加機能コードは変更していない（スクロール矢印修正を除く）。

検証: 本体とprototypeの最終ビルド成功。Browser plugin not availableのためPlaywright、127.0.0.1:4173、1366x768/390x844。矢印の上下スクロール・ホイール、予定追加/停止/再開、個別会話の担当制限、全体停止表示、バックアップ履歴更新、接続デモ、ログイン失敗/成功、モデル一覧とOllamaの推論無効化、モバイル予定追加、横はみ出しなしを確認。pageerror/console error/Vite overlayなし、/apiおよび外部への通信なし。API・実ジョブ・実端末は今回未検証。スクリプト/スクリーンショットはリポジトリ外。
2026-09-07 定期実行の削除: デモのSchedules.jsx/Activity.jsx/App.jsxに予定削除と確認・キャンセルを追加。続けて本体のDELETE API・論理削除・確認画面を実装済み。今後の起動を止めて一覧から取り除き、過去schedule_runsと上限会計・既存仕事・会話・履歴を維持する。最新の125テストと本体ブラウザー確認は冒頭の予定削除節を参照。
