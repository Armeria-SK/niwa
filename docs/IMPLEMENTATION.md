# 本体実装の進捗

2026-09-08: 専用BrowserPageの読取と観測済みリンク遷移を実Chromeの人工ページで検証し、全191テスト成功。変更されたhref/古い版・未分類POST・download/button操作を拒否する。snapshotは主文書の可視DOMだけでiframe/shadow DOM操作は未対応。Linux workerとネットワーク隔離、Botへの接続、承認済み変更操作は残件。

ブラウザー公開取得の追加: 画像等のバイナリを保持するreadPublicResourceと、指定document/静的GETのみ・未知送信拒否・操作ごとの通信量上限を持つBrowserRequestsを追加。公開IP/DNS固定/転送先/中断の既存境界を共通利用。全190テスト成功。本体のブラウザーツール/worker/Podman出口への接続前であり、ブラウザー機能の完了宣言ではない。

最新（2026-09-07）: ユーザーが計画全体の完了へ再開を明示。回数制限撤去bccd378/fde3d92、会話開始前の記憶整理a0db3a5、仕事完了前の出所付き要約f39385cを実装し184テスト成功、PID8800へ反映した。実モデルでmemory_reviewを6体が呼び、2体に各5件の記憶保存を件数のみ確認。ユーザーは以後のサーバー再起動を随時許可している。
ブラウザーは専用Chromium pipeの通信基盤を追加し、3件の人工streamテスト＋実Chromeの一時profile/context/targetで確認。操作worker・ネットワーク出口・Linuxの隔離実機・本体への接続はこれから。X連携・パッケージ導入・実Ubuntuの通し検証など計画残件はなお存在する。全計画完了ではない。細部と最新の反映状況はHANDOFFの冒頭を参照。以下は時系列の過去記録を含む。

ユーザー依頼「長くやりとりしているが記憶がなさそう、調査して」に対する読取調査。実state/control.dbと6体のmemory.dbをDatabaseSync readOnlyで確認。会話163件時点、6体ともmemories0・memory_audit0、保持された非discarded task_steps内のmemory_remember呼出し0。deleted_content記録なし。共通ルールにmemory_remember指定/記憶保存指示は見つからない。モデルへツールは全Bot共通で公開、会話にmessage_idを渡す実装は存在するが、BASE_RULESに記憶保存の具体的契機がなく、conversation_sendで発言終了後に自動で記憶を整理する処理もない。モデル任せの保存選択が実際には行われていない。
人工RuntimeでexecuteTurnTool(memory_remember)→memoryPage.total1→次のcontextに1件→Runtime再作成後も1件を確認。保存/API取得/読込/保持の経路はこの検証では正常。実ユーザーの記憶・会話・設定は変更せず、サーバー再起動/コード修正も行っていない。次に修正する場合は記憶形成の契機と重要事項の選別、出所・公開範囲・重複抑止、既存会話からの整理を検討する。今回の依頼は調査であり旧全体goal再開ではない。


ユーザー新規指定（2026-09-07）: 活動を会話処理履歴から分離。できごとは決定事項、仕事はお金に関するもの、承認待ちは明示承認、履歴タブ不要。成果物へ削除、スレッドへホバー時の削除/ピン留め。これは以前停止した全体計画の再開ではなく今回の限定依頼。
c70ec30で対応。decisionだけをできごとへ表示。business_task_registerで収益化・販売・費用等の具体的仕事を明示登録し、仕事タブはその仕事だけ表示（過去の会話返信は自動で収益仕事へ分類していない）。approval_requestは質問と別に内容・版を保存しwaiting_userで停止、管理者が同じ版を承認すると再開、見送りは中止。古い版/二重回答/未認証を拒否し通常resumeで承認を迂回させない。決済や外部公開そのものの実行機能は追加していない。
成果物DELETE、room DELETEを管理APIへ追加。確認Modalで削除範囲を説明。成果物は本体データと関連できごとを消去。スレッドは会話・関連成果物・会話を出所とする記憶を消去し、進行仕事を中止・予定を廃止、古いモデル出力を無効化。参照整合性用のroom/task ID行は本文を空にしてアクセス不可とし、削除台帳を過去バックアップ復元にも適用する。バックアップの通常保持期間は変更しない。共有作業フォルダー内ファイルは別機能であり今回の成果物削除対象外。
スレッドのピン留め/削除はボタンを入れ子にせず独立配置、hover/focus時に表示、タッチでは常時表示。承認済みの配色・文字密度を維持。176テスト成功。人工API/SQLite＋Chrome/Playwrightの1440/390で決定のみ/収益仕事のみ/履歴なし/承認見送り/成果物取消削除/スレッドhoverピン取消削除、別途hasTouchの390でhoverなしピン操作を確認。Browser plugin not available。実データの削除や実承認はしていない。
実行中/queued0を直前確認しPID22244を停止、通常UIをビルドして同じ.local/ui-runtimeをPID30144で起動。実ログインで履歴タブなし・thread controls1・新APIキーを確認、状態17中止109完了25確認待ち3子待ちを維持、stderr空。実businessTasks/approvalsは0で、実モデルの新ツール利用は未確認。前に未反映だったBot削除65b665d/削除後閲覧71f7c59も同じ更新で反映。今回の変更は完了し、旧全体goalは停止を維持する。


7dda794で仕事詳細Modalの保存中操作をガード。同期refで連続送信を拒否し、通信中は追加指示入力と停止/再開/中止/再試行/完了ボタンを無効化、finallyで解除。成功時のみ入力を消し、失敗時は保持。人工API/SQLite＋Playwrightの1440/390で同一イベント内2クリックでもPOST1件・DB指示1件、他操作の無効化、HTTP500後の入力維持と再送成功を確認。現API互換の.local/hint-compatible-webへActivityだけをコピーし通常dist/clientへビルド反映。PID22244は維持、実状態17中止99完了20確認待ち3子待ち5実行中。実仕事に操作なし。Bot削除65b665dと削除後会話71f7c59は引き続きAPI更新待ちで未反映。


反映の直前確認を更新。実APIでrunning/queued=0（cancelled17/completed98/waiting_user19/waiting_child2）を確認し最新backend/UIをビルドしたが、停止直前の再確認で新しい仕事が開始していたためStop-Process前に中断。PID22244は停止せず継続。配信UIは.local/hint-compatible-webから現API互換版へ戻してビルド成功。再確認はcancelled17/completed98/waiting_user19/waiting_child3/running1。Bot削除65b665dと会話閲覧専用71f7c59は引き続き未反映。次回は配信UIビルドを最後の停止直前確認後に行い、一時的なAPI/UI版の不一致を避ける。計画照合ではプログラム実行はProgramLog/共通ターン接続まで実装済み、実Ubuntu/Podman境界・ブラウザー操作・X連携はなお未完了。履歴中のこれらを接続前とする記述は過去時点。


状態連動アイコンを現在の検証用UIで再確認。人工API・実SQLiteの仕事を作成→claim→completeし、待機none、実行sway、完了animationstart1回、その後noneを確認。活動中Botのままページ再読込し、続く2回のstate応答後も完了演出0回・noneを確認した（休眠による演出抑止とは区別）。動きを減らすOS設定、全体停止、明示float設定後の休眠でも静止を確認。Chrome/Playwrightで成功、追加コード修正は不要。実仕事への操作はなし。


Bot削除後の成果物を追加検証。人工API・実SQLite・Chrome/Playwrightで1440/390px両方について、削除済み主体IDによる作成者絞り込み、本文だけに含まれる語での検索、一覧1件、削除したBotの帰属表示、プレビュー本文、ダウンロード実ファイルの本文一致を確認。ブラウザーはテキスト成果物名へ.txtを補う。既存の削除・履歴保持・仕事操作拒否・個別会話閲覧専用の検証も同時成功。コード変更は不要。実環境でBotを削除していないため、これは人工データによる検証であり稼働中への反映完了を意味しない。


削除済みBotとの個別会話は履歴閲覧専用とし、返信・添付・新規セッションを無効化。削除理由の案内をモバイルでも表示し、共有会話の依頼先候補から削除済みBotを除外、選択済みの宛先も送信時に拒否。人工API＋実SQLite＋PlaywrightのPC1440/モバイル390でBot削除、履歴・成果物保持、仕事中止、再試行非表示、個別会話の入力禁止と案内表示、共有の現役Bot候補を確認。ビルド成功。実APIはcancelled17/completed88/waiting_user13/waiting_child2/running5/queued1のため再起動せず、Bot削除機能と今回UIはまだ稼働環境へ未反映。実Botへの削除なし。


ユーザー指定によりREADMEを通常のシステム全体へのNode.jsインストールへ変更。NodeSource 24系を管理者側で導入し、niwaユーザーへの切替とgit cloneを次の手順に分離。要求バージョン24.16.0以上25未満を明記し、私有runtimeパス・PATH設定・xz-utilsを除去。配布元公式setup_24.xを確認、READMEの全17bash枠の構文検査成功。実インストール・アカウント作成・稼働環境変更は行っていない。


ユーザー再指摘によりREADMEのNode導入スクリプトを5402468で再修正。前回はgit cloneとの分離だけだったため、今回は括弧・set/test・一時変数・trapを除去し、mkdir/cd/curl/照合/tar/versionを説明付きの個別枠に分割。npm準備もPATH/cd/npm ci/buildの別枠へ変更。全18bash枠をUbuntu sh -nで構文確認、実インストールは未実施。


65b665dでBot恒久削除機能を確定（13ファイル141追加17削除、うち回帰テストを含む）。admin専用・リーダー除外・表示時profile hash照合、名前入力確認Modal、進行中仕事と委任先の中止／定期実行廃止、人格と本人tool_receipts削除、私的DBディレクトリ消去。削除済み主体は再招集／仕事再試行不可で保存枠は再利用可能。会話・成果物・仕事履歴を保持しUIの作成者を『削除したBot』に表示する。過去バックアップは通常保存期間で保持するが復元CLIは現在のdeleted_agents台帳を適用して復活を防ぐ。ファイル消去途中の残存は起動時にも消去する。173テストと人工APIブラウザーPC1440/390の確認済み（直前記録に詳細）。別UI変更を混ぜずMembersのみ候補内容から選択コミット。現在実APIでcancelled17/completed80/waiting_user6/waiting_child2/running3を確認したため、稼働中PID22244へは未反映。実Botの削除は行っていない。次の反映は仕事終了を確認してAPI/UI同時に実施する。見た目保存の旧画面残存推定はユーザーの再読込後の返答がまだ無いため、解消を実操作で断定していない。


ユーザーから見た目の保存エラー画像、会話内仕事表示の質問、READMEのパッケージ説明とNode/git clone分離を受領。53b6c70でREADMEへgit/curl/ca-certificates/xz-utilsの用途を追加、Node導入・git clone・npm準備を別ステップ化し全10bashブロックをUbuntu sh -nで検査。画像では新しい再読込ボタンがないため旧画面残存によるprofile PATCH契約不一致と推定、ページ再読込を案内。現行配信画面のprofile_versionと再読込ボタンは実ログイン読取で確認（実プロフィールの保存はしていない）。会話の未返信案内と進行中仕事が同時表示される点を修正し、仕事表示へ専用スタイル追加。人工API＋PlaywrightのPC1440/モバイル390で仕事ありの空案内非表示・仕事なしの案内・横はみ出しなしを確認。開発サーバーは稼働のまま、Bot削除の未反映コードを除いた互換UIを.local/hint-compatible-webから通常dist/clientへビルドして反映。PID22244維持、実仕事に操作なし。実API上で実行中／子待ちを確認し、別の仕事は実行区切り到達の確認待ち（モデル生成のバグと断定しない）。

進行中の別作業: Bot恒久削除を未コミット実装中。deleted_agents表、admin削除／仕事中止／定期実行廃止／profileと本人tool_receipts削除／私的DBディレクトリ消去、起動時残存消去、復元時削除台帳適用、保存枠回復、旧主体拒否。API version照合DELETE、名前入力確認Modal、履歴の削除済み表示と再試行非表示まで接続。173テスト・型チェック・検証用ビルド、人工APIブラウザーPC/モバイルで削除／取消／ファイル消去／履歴維持を確認。実Botは削除していない。バックエンドdistはこの未反映変更を含むため次の再起動前に差分と検証を確認する。通常配信UIにはこの機能をまだ入れていない。


プロフィール関連の7b47c2c/e8fa241/4dbaa68を稼働開発環境へ反映。直前の実APIで実行中・待機列0、cancelled17/completed62/waiting_user3を確認。既存PID28556の起動引数を照合して停止し、通常dist/clientへ最新UIをビルド、同じ.local/ui-runtimeで通信可能な環境からPID22244として起動した。管理者ログインとstate全Botのprofile_version64桁、新しい編集再読込ボタン表示を読み取り確認。状態件数は17/62/3を維持、stderr空。実プロフィールやモデルの保存はしていない。確認待ち3件は再開していない。直前までの『未反映』は履歴。171テスト＋人工API/ブラウザー検証済みコードの反映で、実モデル新規応答は未確認。Ubuntu導入・専用主体作成・実Podman試験の許可は引き続き未受領で、今回の開発サーバー更新とは別。


プロフィールとモデルの同時保存を原子的に変更。profile PATCHへ任意selectionを追加し、ModelGatewayは入力を複製してカタログ／能力・接続世代を確認してから、Runtimeでprofileとmodelを同一制御DB transactionで保存する。UIは1回のPATCHだけを送り、モデル確認失敗でプロフィールだけ保存される状態を解消。編集元hashにprovider/model/reasoningも含め、確認中または別画面でのモデル変更も409で拒否。171テスト・型チェック・バックエンド／検証用UIビルド成功。人工HTTP＋実SQLiteで未導入モデル拒否と不変、確認中の別編集拒否、triggerによるモデル書込失敗時のプロフィールrollback、同時成功、外部モデル変更後の古い保存拒否を確認。PlaywrightのPC1440／モバイル390でモデル確認失敗時のデータ不変と入力保持→同じ入力の再保存成功を追加確認。既存profile／appearance競合・再読込・連続保存も成功。Browser plugin not available。通常dist/clientと稼働PID28556は変更せず未反映。直前記録の『プロフィールとモデルが別操作で片方だけ保存され得る』は今回解消。


プロフィール／見た目の編集競合を防止。管理APIのprofile PATCHは{patch,version}必須、stateで名前とプロフィール本文のSHA256をprofile_versionとして返す。制御DB transaction内で編集元と現内容を照合してから既存無効化と更新を行い、不一致409／version欠落400。内部管理呼出しは任意expected、Bot自己編集の権限は従来通り。UIは編集開始時のversionを保持し成功応答のversionへ更新、古い保存で入力を維持、明示『最新の内容を読み込む』で再読込。モデル変更前にprofile競合を確認（プロフィールとモデルの保存は依然別操作で、モデル変更失敗時にはプロフィールだけ保存され得る）。170テスト・型チェック・本体/UIビルド成功。人工実API＋PlaywrightでPC1440/モバイル390のプロフィール／見た目409、入力保持、最新読込、連続保存、横はみ出しなし・pageerrorなしを確認。Browser plugin not available。証拠.local/profile-conflict-qa.cjsと画像。稼働サーバーPID28556は変更せず、通常dist/clientは現サーバー互換の旧App/components/Profileコード＋既存UI変更から再ビルドして維持した。新UIは.local/profile-conflict-clientで検証、未反映。次の反映はAPIとUIを同時に更新する。プロフィール版別履歴やモデル設定の競合制御は別残件。


管理者によるプロフィール変更時、対象Botの保存済みモデル応答・ツール指示を無効化し本文を消去するよう修正。従来は実行中のlease中断だけで、確認待ち等の保存済み指示が再開時に残った。既存memory_state revisionを更新して旧入力も失効させる。制御DB更新より先にmemory DBをtransactionで無効化するため、途中失敗は余分な作り直しになり得るが古い指示を維持しない。Bot自身のprofile_updateの挙動は従来通り。170テスト・型チェック・バックエンドビルド成功。人工モデル＋実SQLiteで変更前agents_create指示の保存→確認待ち→管理者人格変更→Runtime再作成→再開→新人格で完了／Bot非生成を確認。稼働サーバーは再起動しておらず修正は未反映。プロフィールの版別閲覧・編集競合UIは未実装。


Ubuntu実機用verify-program.mjsを追加。--apply明示と空の検証専用workspaceを要求し、実Podmanで共有書き込み／ホスト私的ファイル・リンク先・OS書き込み・外部通信拒否、cgroup設定値、seccomp／NoNewPrivs、非ゼロ終了、出力上限、期限、中断を確認する。人工データのみ生成し自身の生成物を片付ける。deploy/ubuntu/READMEに実行条件と確認範囲・未検証範囲を追記。.gitattributesでshellファイルのLFを固定。node --check成功、--applyなしの起動が実行前に拒否されることを確認。実Podman未実施。これは検証準備の進捗であり、隔離の実証ではない。Ubuntuへのパッケージ／専用主体導入の許可は引き続き未受領。現在稼働中の本体に変更なし。


READMEを外部利用者向けに全面改稿。最新ユーザー指定でUbuntuのみを案内し、Intel/AMD x86_64の同一PCブラウザー利用を対象とした。niwaユーザー作成、公式ハッシュ検証Node24.16.0、clone/npm ci/build、初回設定、起動、管理者キー、実UI名によるモデル接続、終了／再起動、困ったときを掲載。開発経緯・テスト件数・内部引継ぎリンクを外した。プログラム実行・ブラウザー操作・X投稿は利用準備中と明記。README内8コードブロックはUbuntu sh -nで構文検査成功。CLI引数・画面文言をソースと照合したが、Ubuntuへの新規インストール通し試験は未実施。OS導入／アカウント作成の承認は引き続き未受領で、README更新をその許可と扱わない。実環境・稼働サーバーは変更していない。


Ubuntu実機準備の手順を具体化。WSL登録名Ubuntu-22.04の実OSは/etc/os-releaseでUbuntu26.04.1と判明。/home/niwa/niwaは存在し中身は空。Podman5.7.0候補、systemd PID1、標準Node候補22系。apt-get -sでPodman関連の導入候補を確認したが、インストールは未実施。
deploy/ubuntu/prepare-executor.shを追加。明示--apply・root・x86_64・空の製品ルート・未作成の専用主体を要求する。OS依存導入、公式ハッシュ検証Node24.16.0の製品内配置、niwa-exec／niwa-ipc作成、必要ディレクトリのみの所有権・親通過ACL設定を行う準備用コード。サービス起動・認証・イメージ取得・Windowsマウント変更は含まない。WSL sh -n検査成功。実行は未許可・未実施。ユーザー提供AGENTS.mdのdeployment/account operations非許可に従い、OSパッケージと専用主体作成の承認をこれから求める。承認が無い間はこのスクリプトを実行しない。
コードの実行機能は169テスト成功時から変更なし。稼働WindowsPID28556は維持。現在の質問は実機準備への許可であり、Windowsネイティブ対応への方針変更はユーザーから指示されていない。


Ubuntu隔離実行の起動前検査を追加。executorは受付前にPodman infoのrootless／非remote／seccomp／cgroup v2のcpu-memory-pids／HOMEとOSruntime内のコンテナ保存先を検査し、指定sha256イメージがローカルにあることも確認する。未導入イメージを自動取得しない。169テスト・型チェック・バックエンドビルド成功（人工Podman情報の不適合拒否。実Podman試験ではない）。
読み取り専用deploy/ubuntu/check-prerequisites.shと配置・実機受入手順READMEを追加。実Ubuntu-22.04へ標準入力で診断を渡し、Linux・非root niwa(uid1000)・cgroupファイル・Windowsドライブ未マウントを確認。node/podman/niwa-execなしでexit1。Windowsファイルへの/mnt/d経由アクセスは不可だった。パッケージ導入、OSユーザー作成、マウント変更、本番配置は未実施。
ユーザー質問『npmがあればWindowsでも同じ動作か』へ、基本機能は現Windowsで稼働中だが隔離実行／専用ファイルサービスはLinux実装、Windows PCではWSL2が必要と回答。Windowsネイティブ隔離対応を実装した、あるいはnpmだけで全機能が同じとは扱わない。稼働PID28556は変更していない。


プログラム実行の再開を「照合のみ」に制限。Tasks.externalOnceは保存済み操作か新規操作かをサーバー側で区別してcallbackへ渡す。program_runは初回だけallow_start=true、保存済みの実行IDはfalseでサービスへ送る。ProgramLogはfalseの要求で記録が見つからなくても新規実行や新規意図の保存をしない。既存結果は通常通り返す。この指定はモデルのツール引数にはなく、追加しようとすると拒否する。既存の共有ファイル処理は引数追加を無視し従来通り。
168テスト・型チェック・バックエンドビルド成功。実SQLiteから実行記録を消した状態で、共通ツール／実IPC経由の仕事再開が確認待ちを維持し、runner呼出し1回のままであることを確認。未実行の照合が記録を作らないこと、保存済み結果の照合、モデルからのallow_start注入拒否も試験。これは本体の操作記録が残る場合の保護であり、本体・実行サービス両方を過去世代へ戻した場合の保証ではない。
現在のWindows稼働環境にはprogramExecutorUidを追加せず、PID28556も再起動していない。実Podman／Ubuntuでの隔離試験・ディスク上限・本番構成は引き続き残る。


隔離プログラム実行を本体の共通ターンへ接続（設定時のみ有効）。programExecutorUidを明示したLinux構成でprogram.sockの専用主体・パス・権限を毎回検査する。共有ファイルクライアントとendpoint検査を共用。IPCは実行ID付き応答を照合し、1MiB応答・各64KiB出力を上限として不正な形／別実行IDを拒否する。通信の自動再試行はしない。
program_runは接続設定あり・共有会話の場合だけ公開し、モデルからの主体ID・マウント指定は受けない。本体が仕事／会話／Bot／実行IDを付与し、既存externalOnceへ接続。仕事期限・Schedulerの中断をIPCまで伝える。出力はuntrustedとして次のモデル入力へ返す。結果不明は既存のwaiting_userへ移し、同じ実行IDの記録を照合して再開する。個別会話・期限切れ／停止・不正引数は送信前に拒否する。
166テスト・型チェック・バックエンドビルド成功。人工モデル／人工runner＋実SQLite／実named pipeで共通ツール→サービス→結果→仕事完了、非共有拒否、完了済み再送非実行、一時停止の中断伝播と再開時のpending照合を確認。IPCの別ID／巨大応答／不正形／切断・保護確認失敗も試験。稼働Windowsには設定を追加せず、PID28556は再起動していない。実Podman／UbuntuのOS境界、実モデルによるプログラム遂行、専用実行DB紛失・異世代復元の照合、ディスク使用量制限は残件であり、この機能を本番利用可能とはまだ扱わない。


ユーザー指定のBot並列実行を反映。調査ではSchedulerは元々Bot間並列（同じBotの仕事は1件ずつ）、稼働concurrencyLimit=nullだったが、サブスク問い合わせがアカウント共通ConnectionGateで直列だった。CodexConnection.createが仕事ごとに独立したadapter／継続状態を作るため、そのホスト側ラッパーを並列対応に変更。接続・Gateway双方の待ち列を通らず問い合わせを並列送信する。生adapterの既定能力やparallel_tool_callsは変更せず、OAuthAccountの資格情報更新調停・利用上限時の確認間隔・既存の同時実行設定は維持。
163テスト・型チェック・バックエンドビルド成功。人工HTTP＋実adapterで3件同時進行、1件中断と他の継続、別Botの本文非混入、1件時間切れ中の別仕事完了、並列401で認証更新1回を確認。実接続先が複数同時応答を受理する通し試験はまだ未実施。実行・キュー0を確認し同じ.local/ui-runtimeを通信可能な環境でPID25348→28556へ再起動。中止17・完了62・確認待ち3を維持。既存の確認待ち仕事は再開していない。


専用プログラム実行サービスを追加。entrypoints/executorはLinux非root・専用の私的state/home/runtime・共有領域外の保護Unixソケットを要求する。既存のプロセスロックを取得し、保存された未完了コンテナ名だけをPodmanへ回収要求してから受付開始（結果はunknownのまま、再実行しない）。起動時に既存ソケットを削除しない。保護ディレクトリと0600系state・0660ソケットを使用し、HTTPネットワークへ公開する起動経路を設けない。
serverはPOST /programsに限定し、128KiB入力・最大4受付、未知の引数拒否、実行記録による再送処理、通信切断時のAbort、停止時の全実行Abortと完了待ちを実装。内部例外本文は返さない。162テスト・型チェック・バックエンドビルド成功。Windows実named pipe＋実SQLite＋人工runnerで再送結果、スコープ変更拒否、不正形式／上限、切断・停止を検証。Windowsからのexecutor起動は固定エラーとexit1で拒否。
実Ubuntuでの専用主体・Podman・再起動回収・OS隔離は未検証。本体からのIPCクライアント、Bot仕事・停止・結果回収への接続はまだ未実装。サービスをUbuntuに配置／起動したという意味ではない。既存の稼働WebサーバーPID25348は変更していない。


隔離プログラムの永続実行記録ProgramLogを追加。専用DB（共有領域外へ配置する契約）で同期FULLの実行意図を先に保存し、同一実行IDの同時再送・再起動後の再送は開始しない。要求のBot／会話／仕事・コマンド・時間・信頼する実行環境キーをハッシュへ含め、内容変更を拒否する。コマンド配列はawait前に複製する。成功／非ゼロ終了の出力を保存し再送時に返す。中断・出力保存失敗はoutcome_unknownを維持し、自動再送しない。コンテナ名も先に保存し、後の回収処理で同じ対象を識別できるようrunnerへ渡す。
159テスト・型チェック成功。人工runner＋実SQLiteで再起動後の結果再利用、別スコープ／変更要求拒否、2接続の同時配送、入力配列変更、途中中断、実行前／結果保存時のDB失敗、事前キャンセル、閉じたDBを検証。現時点では記録部品であり、実Podman・専用サービス・Botツール・管理画面の確認待ちへの接続、起動時コンテナ回収、失われた実行DB／異世代バックアップの照合は未実装。実行結果不明が画面に表示されるとはまだ扱わない。稼働サーバーPID25348は変更していない。


隔離プログラム実行の最初の部品を追加（src/sandbox/program.ts）。信頼する専用Linux実行主体だけが固定Podman・ローカルsha256イメージを呼ぶ。生成コードをホストシェルへ渡さず、明示したentrypointと引数をコンテナへ渡す。共有/workspaceだけをbind、network=none、非root、capability除去、読取専用OS、一時/tmp、CPU／メモリ／PID／出力／1〜300秒の上限を固定。ホスト環境変数はHOME・XDG_RUNTIME_DIR・PATHだけを選択。中断後は別の期限でコンテナ強制削除を試み、削除失敗は結果不明として失敗する。実行は自動再送しない。
155テスト・型チェック成功。検証は引数境界と人工Podman呼出しによる非ゼロ終了／中断／削除失敗であり、実Podmanの隔離を証明しない。Botツール・HTTP・実行サービスへは未接続。永続実行記録、再起動時の照合、専用サービス、ディスク使用上限、OS隔離の迂回試験が残る。現在の稼働サーバーPID25348は変更していない。
環境の読み取り確認を更新: 制限外でWSL Ubuntu-22.04の存在、非root niwa(uid/gid1000)、cgroup controllersを確認。Ubuntu内のpodman/nodeはcommand -vで見つからなかった。Ubuntu自体が存在しない、または状態取得不能という以前の記述は過去時点。インストール・専用OSユーザー作成・本番配置は行っていない。
仕様参照: https://docs.podman.io/en/latest/markdown/podman-run.1.html （--timeout、--pull=never、--read-only等）。


会話履歴の分割取得を実装・反映済み。最初の発言は固定ヘッダーへ維持し、返信は最新50件から過去へ追加取得する。Appの全会話本文の定期取得を除去し、stateへ件数・最初／最後の日時・最終連番のみ追加。本文は表示中の会話だけ取得し、新着時に更新。未読込の過去本文もサーバー検索の対象。会話切替はkeyで状態を分離し、遅延した古い応答を無視する。古いページ追加時のスクロール位置、ピン順、返信件数を維持。
152テスト・型チェック・本体ビルド成功。人工123返信でページ重複／欠落なし、個別会話認可、%_リテラル検索、カーソル、不正入力、再起動を検証。実API＋PlaywrightのPC1440／モバイル390で分割取得、固定ヘッダー、過去本文検索、新着・大量新着、古いページ取得中の新着、会話切替中の遅延応答、失敗／再試行、横はみ出しなしを確認。Browser plugin not available。
実行・キュー0を確認し、同じ.local/ui-runtimeを通信可能な環境でPID15496→25348へ更新。中止17・完了62・確認待ち3を維持。稼働画面のログイン・会話表示・メンション強調32件／先頭二重0を読み取り確認。人格の版管理・Bot恒久削除、ブラウザー操作・隔離プログラム実行・X連携、本番Ubuntu検証などは引き続き残件。


記憶の取得を表示中のBotだけの50件ページに変更し反映済み。全Botの記憶全文を2秒ごとに取得するApp処理を除去。stateは本文を含まないmemory_version（監査番号）を返し、選択中の記憶画面は作成・訂正・削除の変更時に読み込み済みページを再取得する。作成監査番号のカーソルで続きへ進むため、カーソル対象の記憶が削除されても続きの取得を維持。検索は未読込ページを含む全記憶の文字列検索。Bot切替や検索変更後の古い通信結果を無視する。編集時の表示版を保存APIへ渡し、別画面の訂正を無条件で上書きしない。
151テスト・型チェック・検証用／本体ビルド成功。人工123件の全ページ取得、検索・%_リテラル、境界削除、別Bot拒否、版変化、再起動保持を検証。実APIのPC1440/モバイル390で50→100→123件、未読込ページの検索、古いページ更新、訂正・履歴・削除・新規記憶反映、古い編集の409拒否、通信失敗／再読込、Bot切替中の遅延応答非混入を確認。Browser plugin not available。
実行・キュー0を確認して同じ.local/ui-runtimeを通信可能な環境でPID29176→15496へ再起動。中止17・完了62・確認待ち3を維持。稼働画面も記憶ページ取得1回・全件取得0回・エラーなしを確認（対象Botの実記憶0件、多件数は人工データ検証）。メッセージのページング等は引き続き未実装。

待機中の仕事にも「この仕事を一時停止」を表示（Activity.jsx条件の2行変更）。既存APIを使用し、接続待ち／回答待ち／仲間の結果待ちの3状態に対応。人工DB＋実APIのPlaywrightで3状態×PC1440/モバイル390の停止・再開・中止、停止中の接続自動再試行抑止、子の完了後も親のpausedを維持して実行しないことを確認。検証用・本体ビルド成功、バックエンドは150テスト成功時から変更なし。通常dist/clientへ反映済み、サーバー再起動不要。稼働画面の確認待ち仕事に停止ボタンが出ることを読み取り確認し、実仕事は操作していない。状態は中止17・完了62・確認待ち3を維持。Browser plugin not available。

ユーザーの＠メンション二重表示修正を反映済み（751371f）。Bot本文に宛先がある場合は配送処理が先頭の既存宛先を除いてから1回だけ付与する。複数宛先・全角＠にも対応。既存の先頭の同一二重メンションは保存本文を変更せず表示だけ1つにする。会話中の既知Botメンションと選択済み宛先へ背景色と太字を追加。通常本文はReactの文字列として表示し、HTMLは実行しない。150テスト・型チェック・本体ビルド成功。人工APIのPC1440/モバイル390で背景／太字、二重解消、全角、正規表現記号入り名、メール非誤認、HTML非実行を確認。稼働中の該当会話も読み取り確認し、強調32箇所・先頭二重0・fontWeight700。
実行・キュー0、確認待ち3を確認し、PID20292を停止して同じ.local/ui-runtimeを通信可能な環境でPID29176として再起動。ログインと中止17・完了62・確認待ち3の維持を確認。成果物検索109cf56、仕事履歴42f00e2、一括確認16c7e1aも今回反映済み。
併せて記憶の訂正履歴93d0c46を反映。選択記憶の管理者用corrections APIで訂正者・日時・版の直近100件だけ返す。古い本文を返さず、存在する記憶に限定。管理者権限、別BotのID拒否、上限・再起動保持・削除後404を自動検証。人工APIの画面で訂正保存→履歴、外部訂正時の更新、削除時閉鎖、PC／モバイルを確認。Browser plugin not available。以下の未反映記録は過去時点。実モデルによる修正後の新規投稿はまだ未確認。

できごと一括確認の上限不具合を修正。未確認1003件の1要求が400で全件未確認のままになることを実APIで再現。App.readUpdatesを1000件ずつの逐次送信へ変更（差分3行）。実APIのPlaywrightで1000+3の保存、操作中に新規到着した質問は未確認のまま、再読込保持、個別確認、確認済み表示、390px横はみ出しなしを確認。検証用ビルド成功。バックエンドは149テスト成功時から変更なし。追加のスレッド整理QAでPC1440/モバイル390、ピン／保管の再読込保持、保管中返信禁止、保管から選択して復帰、本文／仕事状態保持を確認。Browser plugin not available。稼働環境は実行3・完了49・中止17を確認、今回もdist/client更新と再起動は未実施。成果物検索109cf56・仕事履歴42f00e2・一括確認修正の反映はアイドル確認後の通常ビルドと通信可能な環境での再起動待ち。

仕事詳細の操作履歴を実データへ接続。GET /api/tasks/:id/historyは既存の仕事・会話閲覧権限を確認し、直近100イベントを時系列で返す。詳細を開いた時と仕事の更新／追加指示件数の変化で再取得し、日時と日本語の状態・操作名を表示。取得失敗は明示。149テスト・型チェック・検証用ビルド成功。人工DB＋実APIのPlaywrightで一時停止・再開・指示・中止・再試行の反映、詳細再表示、PC1440/モバイル390を確認。最初の画面試験で待機中の追加指示がupdated_atを変えないため履歴が更新されないことを発見し、指示件数も更新条件に加えて再検証成功。閲覧範囲、100件上限、順序、再起動保持は自動テスト済み。Browser plugin not available。稼働3210は実行3・キュー1・完了44・中止17、再起動せず。成果物検索109cf56と今回の履歴は通常ビルド・アイドル時再起動待ち。dist/clientは旧版を維持。

状態連動アイコンの追加検証: 人工DBと実APIポーリングを使う.local/motion-qa.cjsで、初期静止→実行中sway→完了animationstartが1回→静止を確認。次の仕事が始まっても過去の完了を再生しない。prefers-reduced-motionでCSS animationName=none、全体停止でnoneを確認。停止解除後に明示floatが動くことを確認してから休眠へ移しnoneとなることも確認した。Browser plugin not available、通常Playwright＋Chrome。コード修正不要。実ユーザーDBは読み取りのみ、最新running3/completed42/cancelled17のため成果物検索109cf56は引き続き稼働画面へ未反映。

成果物本文検索を追加。GET /api/artifacts?query= は名前・説明・本文を検索し、一覧は本文を含まない。既存の会話閲覧範囲を維持、200文字上限・空白・英字大小・%/_のリテラル一致を検証。UIは200ms待機、古い検索結果の無視、検索中／失敗表示、作成者と種類の絞り込みを維持。148テスト・型チェック・検証用ビルド成功。人工DB＋実APIのPlaywrightでPC1440/モバイル390、本文のみの一致、認証、上限、絞り込み、素早い入力変更、通信失敗を確認。Browser plugin not available。稼働3210はrunning3を確認したため再起動せず、既存dist/clientも更新していない。検証用UIは.local/search-client。反映にはアイドル確認後の通常ビルド＋サーバー再起動が必要。

ユーザー指定でモデル応答上限を120秒から600秒へ変更。外側のストリーム監視は605秒。接続の順番待ちを含む600秒で、仕事全体の期限や停止権限は維持。148テスト・型チェック・ビルド成功。3件がwaiting_providerで実行0を確認し、PID2200から20292へ同じ.local/ui-runtimeで再起動。ABORTED誤分類修正も反映済み。対象のモブA/B/Cの3仕事だけ管理者APIで再開し200・running3を確認。600秒経過までの実モデル通し検証は未実施。


ABORTED調査: ユーザー提示19:21のA/B仕事は起動から120.4秒でwaiting_provider、保存済みモデルstep/ツール実行0。Cはweb_read1件実行後に待機。ConnectionGateの120秒タイマーを内部collectModelEventsがABORTEDへ分類する不具合を人工timeout20msで再現。タイマー起因のみTIMED_OUTへ補正し、本来の外部キャンセルはABORTEDを維持。148テスト・型チェック成功。現行サーバーPID2200へは未反映（作業中のため再起動していない）。接続は直列で順番待ちも120秒枠を消費する設計が残る。失敗時の生のイベントは未保存なのでAPI側事情の追加断定はしない。


最新: 共有フォルダーのPNG/JPEG/GIF/WebPプレビューを追加。拡張子でなく取得バイトの署名で形式を選び、既存認証APIの取得内容をimgへ表示する。8MiBの既存上限と元バイトのダウンロードを維持。画像が壊れていればダウンロード案内へ切替。SVG/HTMLは画像として実行しない。ビルド成功、人工ファイル＋実IPC/APIのPlaywrightで4形式×PC1440/モバイル390、元バイト一致、破損画像、偽PNG内HTML非実行、既存の検索・階層移動・上限・領域外拒否を検証。Browser plugin not available。実共有フォルダーの画像は未検証。フロントのみ変更、サーバー再起動不要。


## 最新: 宛先のあるBot会話と会話画面

返信欄の@候補から1名を指定し、そのBotへ仕事を配送する。Botの最終発言も先頭の@名前から同じ会話内の一意な宛先へ配送。リーダーの代理報告を挟まず、本人が応答する。Bot同士には@宛先・本文の自分の名前ラベル不要・他Botの代筆や復唱をしない共通ルールを追加。名前が重複・不明なら自動配送しない。候補選択なしのユーザー手入力@の自動解釈は未対応。現在は複数候補を保持し、全宛先の仕事を同時作成する。Botはconversation_sendで宛先ID配列を渡せる。平文の共通ルールも追加。147テストで複数配送・再送・分岐全体16回上限を確認済み。以下の144件/1名限定は旧段階。

継続仕事は元仕事との関係・期限を保存。自発活動の停止、休眠、アーカイブを尊重し、16回で確認待ち。仕事の再開で次の区切りを始められる。モデルは休息を選べる。144テスト・型チェック・ビルド成功。再起動・再試行・非共有領域への拒否・再送・上限再開を人工モデルで検証。実モデルの会話品質と指示追従は未検証。

会話の日付・操作・最初の発言を固定ヘッダーにして返信だけスクロール。新規セッションボタンで現在の公開範囲・相手を初期値に新規作成できる。スレッドはピン優先、各区分の最終発言降順。PC・スマートフォンで作成、順序更新、ヘッダー固定、@選択・配送を検証。

## 最新: 実認証確認と失敗段階の表示

実開発環境でChatGPTサブスク認証connected=trueとモデル一覧取得を確認。リーダーのAstraはこの一覧に無く、ユーザー指示によりSol mediumを保存。直近会話の仕事完了はモデル変更前に確認できたが、Solによる応答はまだ未確認。

認証失敗は受信・通信到達不能・交換拒否／時間切れ・保存領域で固定メッセージを返す。生のエラー本文・認証情報・ローカルパスを表示しない。人工認証で通信拒否・HTTP拒否・保存失敗の区別と秘密非表示を検証し、141テスト・型チェック・ビルド成功。起動中サーバーの再起動は未実施。

## 最新: 共有フォルダーの一覧・プレビュー・ダウンロード

成果物画面から共有作業フォルダーを開き、階層移動・現在のフォルダー内の名前検索・再読込・本文プレビュー・ダウンロードができる。管理者認証済みAPIから専用ファイルサービスを経由し、アプリが個別記憶やホストの任意パスを直接開かない。未接続時は画面に表示する。

ダウンロードは8MiBまで、本文はUTF-8かつ64KiBまで。HTMLもテキストとして表示し、実行しない。プレビューと保存には同じ取得済みバイト列を使う。一覧は既存の200件上限を維持し、一部表示の場合は明示する。画像表示は上記の最新版で対応。編集・アップロード・削除は未実装。

140テスト・型チェック・ビルド成功。バイナリー一致、上限超過、領域外参照、差替えパス・破損内容・不正base64応答を検証。Windowsの人工共有領域＋実IPC／管理者APIでPC・モバイルの階層移動、検索、HTML非実行、テキスト／バイナリー保存一致を確認。Browser plugin not availableのためPlaywrightを使用。Ubuntu実行ユーザー分離・実本番共有領域は未確認。

## 最新: 新規Botの標準モデルとリーダーモデルの設定

generated_modelに管理者指定のprovider/model/reasoningを保存し、リーダーがBotを生成するときに読み取る。初期値は既存と同じCodex gpt-5.6-luna/max。変更は以後の生成に適用し、既存の個体・人格・記憶を変更しない。設定画面から、リーダーには既存のモデル変更API、新規Botには専用の取得／保存APIを使用する。

既存のモデル検証を共用し、Ollamaは導入済みローカルモデルとnative、Codexはアカウントのカタログで確認したモデル／推論強度だけを保存する。非対応モデル／強度を拒否し、検証中に接続先・アカウントが変われば保存しない。リーダー自身に標準モデル変更権限は与えない。

138テスト・型チェック・ビルド成功。未導入・非対応・古い接続先の拒否、管理者権限、新規適用・既存保持・再起動後の生成を検証。人工HTTP／認証ストア＋実API／DBで、Ollama標準保存→新規生成、Codexリーダーの推論強度保存、PC／モバイル再表示を確認。実モデルの稼働・品質・本番アカウント接続は未確認。

## 最新: 日次バックアップの実行時刻

settings.backup_timeへ日本時間のHH:mmを保存し、管理者APIと画面へ接続。初期値はデモと同じ03:00。サーバーのタイムゾーンに依存せず、UTC+9の日付境界と設定時刻で判断する。予定時刻前は待機し、時刻後で当日の保存がない場合に実行する。停止した日数分をまとめて作らず、当日の手動保存も日次保存済みとして扱う。保存後の時刻変更・再起動で自動保存を重複させない。

既存の圧縮・スナップショット・世代削除を維持。確認処理の同時起動をまとめ、手動保存が一覧取得中に完了した場合も重複を防ぐ。終了時は進行中の確認と保存を待つ。

137テスト・型チェック・ビルド成功。時刻前後、再起動、変更、日付跨ぎ、停止日、手動保存との競合を人工時計＋実ファイルで検証。人工DB＋実API／Backupsを使い、PC／モバイルの時刻保存・再読込・再変更・手動圧縮バックアップを確認。日次バックアップを無効にする機能や別タイムゾーンの選択は追加していない。本番Ubuntuでの運用・権限分離は引き続き未確認。

## 最新: 自発活動の全体許可スイッチ

settings.autonomousを追加し、設定画面・管理者APIへ接続。オフでは自発予定を新規起動せず、実行中の自発仕事とその子孫のリースを失効してqueuedへ戻す。通常の依頼と通常の定期実行は対象外。仕事の取得・モデル／ツール実行前のリース検査でも祖先の自発予定を確認する。オンで既存の作業状態から再開し、個別停止・中止・期限を上書きしない。

初期値は従来の表示と許可済みの活動方針に合わせたオン。機会を与える予定を新規に作る動作は追加していない。管理者以外は設定を変更できない。136テスト・型チェック・ビルド成功。自発予定の抑止、委任先への中断伝播、通常依頼の継続、再起動保持、同じ仕事からの再開を検証。人工DB＋実APIのChromeでPC／モバイルの保存・再読込・切替を確認。実モデルでの自発活動の品質と実環境隔離は引き続き未確認。

## 最新: 共通ルールの編集・保存・全Botへの反映

管理DB common_rulesに現在の本文と版番号を保存。GET stateへ追加し、PUT common-rulesは管理者だけが期待版を指定して更新する。最大20,000文字、空欄で追加指示解除、同文保存では版を増やさない。別の画面の古い編集は409で拒否する。初期文面は承認済み画面にあった共通指示を使用。

共通ターンで固定の権限制約の後、各Botの人格・記憶の前に指示を組み込む。保存時に現在のリースを失効して応答を取り直す。Bot個別DBのモデル交換へrules_revisionを追加し、再開時に古い版の交換を破棄して未実行のツール指示を実行しない。完了済みの外部操作記録・成果・仕事状態は保持する。ルールは私的記憶として保存せず、モデルが管理者設定を変更するツールも追加しない。

135テスト・型チェック・ビルド成功。管理者権限、古い版拒否、再起動保存、応答中の変更による旧回答拒否、旧版のBot生成指示を実行しないことを検証。人工DB＋実APIのPlaywright／ChromeでPC・モバイルの連続保存、再読込、競合、最新内容への戻し、空欄解除と横溢れなしを確認。過去ルール本文の一覧／復元UIは追加していない。実モデルの指示追従品質と本番隔離検証は未確認。

## 最新: 接続設定・利用上限待ちの自動再確認

tasks.provider_retry_atを追加し、共通ターンの接続解決失敗またはQUOTA_EXCEEDEDで1分後を保存する。Schedulerが期限内・未停止・活動中Bot・未アーカイブの仕事だけをqueuedへ戻す。認可・実行枠・モデル呼出し予算は従来経路で再検査する。モデル設定／アカウントを改めて取得するので、上限期間中にサブスクへ再送せず、指定済みOllamaを使える場合だけ継続する。回復確認時刻を過ぎれば既存のアカウント直列化・確認間隔制御に従って試す。

回答待ち、通常のモデル通信失敗、応答不完全、ツール能力不足による待機を無条件に再開しない。状態遷移で古い再確認日時を消去する。停止解除は利用者の操作に従う。移行前のwaiting_providerには理由文から再確認可否を推測せずNULLのままにする。

133テスト・型チェック・ビルド成功。権限、確認時刻、再起動、全体／仕事停止、休眠、アーカイブ、中止、期限切れ、手動待機を検証。人工HTTP＋実アダプター＋Schedulerで、上限待機→再起動→切替先設定→同じ仕事の完了・一件の投稿・一回のローカル呼出しを確認。実アカウント・モデルは未使用。回復確認が通常通信エラーになった後の自動継続、実モデル検証等は残る。

## 最新: 切替先設定と直近モデル接続の表示

設定から保存済みOllama接続先の導入モデルを取得し、ツール対応を確認して切替先を保存・解除する。URL保存とは独立した操作。旧「準備中」の表示を置換した。待機済み仕事は活動画面から手動再開する旨を表示。

管理DBのmodel_routesへBotごとの直近応答試行先・モデル・理由・日時を保存。モデル選択だけでは記録せず、応答試行開始時に記録する。接続成功や稼働状態ではない。管理者専用APIで設定モデルと合わせて返し、設定画面で更新表示する。アカウントID・トークン・応答本文は含めない。

130テスト・型チェック・ビルド成功。切替／再起動／復帰の記録、Botによる記録読書拒否を検証。Playwright＋Chromeで人工HTTPと本体API・実DBを使用し、PC／モバイル幅の保存・解除・再読込・非対応モデル拒否・横溢れなしを確認。実アカウントは使っていない。待機中仕事の自動再開と回復確認失敗時の運用は引き続き残件。

## 最新: サブスク上限時の切替と回復確認

管理DB移行19にprovider_limitsを追加し、資格情報のアカウントIDのSHA-256、次回確認日時、版だけを保存。上限応答時刻は回復の証明にせず、次の試行を許可する時刻として扱う。提示がなければ15分後、過去の提示でも最短1分後に確認する。期限後の確認は1分間に1件へ制限し、成功したモデル応答でのみ対応版を解除する。新しい上限記録は古い確認の成功では消えない。

ModelGatewayは上限中に指定Ollamaを再検査して選択し、元のBotモデル設定を書き換えない。共通ターンはQUOTA_EXCEEDEDだけを切替対象とし、同じDB作業状態と完了済み交換を保って次のモデル呼出しへ進む。各試行は既存の予定予算へ計上する。Ollamaで継続中は応答区切りで再選択し、回復確認のタイミングなら元のサブスクを試す。通常429、通信／認証エラー、切替先未指定や能力不足は理由付き待機にする。接続確認中の設定・アカウント・記憶変更を検査する。

130テスト・型チェック・ビルド成功。人工HTTP＋実アダプター経路で上限→指定ローカルの回答、再起動後の上限保持、回復後のサブスク回答、設定モデル保持を確認。通常429の非切替、切替先なしの待機、アカウント範囲・権限・古い成功の拒否を検証。実アカウント操作・実モデルは未使用。画面上の実接続先／理由の表示、待機中の仕事の自動再開、回復確認が通信失敗した場合の継続運用は残件。

## 最新: 関心に応じた活動と休息

管理DB移行18で予定のautonomousフラグ（既定0）を追加し、APIの任意booleanと画面の「関心から活動を選ぶ」を接続した。管理者が指定したBot・共有会話・方針・起動条件・間隔・起動数／モデル上限の範囲で活動機会を作る。既存予定を自動で有効化せず、個別会話では設定を拒否する。

work_state.autonomousを保存元から判断し、共通システム指示へ活動選択の方針を追加。プロフィールに記した関心、会話、検索できる過去成果からモデルが活動を選ぶ。必要がなければtask_restでその回を完了し、固定の休息結果と仕事イベントだけを残す。投稿とdone通知は作らず、起動数／モデル消費は戻さない。休息は自発仕事の本人だけが選べ、通常依頼と委任先では非公開・拒否。他のツールと混ぜた休息は実行しない。ツール非対応モデルでは送信前に接続待ちにする。

127テスト・型チェック・ビルド成功。人工モデルで関心付き文脈と休息ツール、静かな休息→後の活動、予算継続、通常依頼の休息拒否、ツール非対応時の送信なし待機を確認。既存移行の無効値と個別会話拒否も検証。Playwright/Chrome・1920×1080／390×844で共有変更条件と自発活動の選択・保存値・表示を確認し、削除操作等の既存フローも通過。実モデルによる活動の適切さ、成果の質や休息判断は未検証。

## 最新: 予定削除APIと確認画面

管理DB移行17で削除フラグを追加し、管理者DELETE `/api/schedules/:id`を実装。予定を無効化して一覧・起動・再開から除外し、予定本文と検知版を消去する。IDの削除印と会計参照を残す論理削除により、遅れた作成再送による復活を拒否し、実行済み／進行中の仕事・会話・履歴と呼出し予算を維持する。削除の再送は成功として扱う。これは既存仕事の停止・削除ではない。

デモと同じ「この予定を削除しますか？」の確認・キャンセルを本体に接続した。125テスト・型チェック・ビルド成功。削除権限、再送、再起動、再作成拒否、既存リースと予算の保持を検証。Playwright/Chrome、1920×1080／390×844でキャンセル・確定削除・再読み込み後の一覧・実DBの仕事／できごと保持・コンソール・横はみ出しを確認。実モデルは使用していない。

## 最新: 共有会話の変更による起動

管理DB移行16でtrigger_kind（interval/shared_changes）とsource_revisionを追加。既存の定期間隔を維持し、APIとフォームで共有会話の変更条件を選べる。個別会話への変更条件はサーバーで拒否する。確認間隔・起動回数・モデル予算・停止・休眠・アーカイブの制限を共用する。

登録時点の、自分以外による共有発言のID／著者／本文からハッシュを保存。確認時に変わった場合だけ仕事を作る。追加・訂正・削除を検知するが、変更のない確認ではモデルを呼ばず次回確認日時だけを進める。自分の返答は自身の次回起動を誘発しない。版と仕事と発火記録は同じトランザクションに含める。モデルには実行時点の正本会話を既存経路で渡す。

124テスト・型チェック・ビルド成功。実DB再起動、保存失敗、停止、個別会話拒否、訂正削除、共通ターンでの応答と自己再起動防止を人工モデルで検証。Playwright/Chrome・1920×1080／390×844で条件選択、共有会話のみの候補、保存値を確認。実モデル・関心に基づく活動選択・休息・金額予算は残件。

## 最新: 定期実行のモデル呼出し総上限

管理DB移行15でmax_model_callsとmodel_callsを追加。予定作成APIの任意max_model_calls（1〜100万、未指定時はmax_runs×24）と画面の入力／累計表示を接続した。既存予定の保存再送ハッシュ互換を保ち、過去の未観測呼出しは推定せず移行時から集計する。

共通ターンがadapter.run直前に、現リースと会話を検査して予約をトランザクション保存する。親タスクを辿り、定期実行の委任先も同じ予算へ計上する。失敗・中断・予約直後の終了でも返却しないため、これは実課金回数ではなく保守的な送信試行の上限。保存済みの応答を再生する場合は追加消費しない。上限到達後の送信は拒否し、予定停止と理由付き待機／できごとを記録する。予定再開でも累計は戻らず、予算が尽きた予定は再開できない。

121テスト・型チェック・ビルド成功。委任・旧主体拒否・再起動・通知重複防止・モデル接続失敗後の追加呼出し拒否・既存予定の移行を検証した。Playwright/Chrome、ローカル人工DB/APIで1920×1080／390×844の設定保存・再表示を確認。金額の上限、実トークン合計、プロバイダー内部のHTTP再試行数は扱わない。実モデルの課金確認は未実施。

## 最新: 定期実行の停止を「できごと」へ通知

起動上限または3回連続未完了で自動停止する際、同じトランザクションでupdatesへ停止理由・会話・担当Bot・最後の仕事IDを記録する。起動上限は決定、連続未完了は確認事項として既存のできごと画面へ表示する。次回予定時刻より前でも、既存仕事がすべて終われば判定する。通常のtickでは通知を追加しない。118テスト・型チェック・ビルド成功。通知保存失敗のロールバック、仕事参照、再起動後の通知重複防止、失敗種別を検証。ブラウザーへのプッシュ通知・メール等は実装していない。

## 最新: 定期実行の管理画面

活動の既存タブ列に定期実行を追加。予定一覧で依頼・Bot・間隔・累計起動数・期限・次回日時・停止理由を確認でき、追加フォームと停止／再開をAPIへ接続した。間隔は分・時間・日・週間で入力する。初回日時は端末の時間帯を明記しUTCへ変換する。個別会話は参加Botのみ、休眠Botとアーカイブ会話は新規選択から除外。送信中の二重操作を防ぎ、同じ入力の保存再試行は同じIDを使用する。

117テスト・型チェック・ビルド成功。Browserプラグイン不在のため既存Playwright + Chromeを使用し、ローカル人工DB/API、1920×1080と390×844で確認。表示・過去日時のエラー・保存後のDB値・再読込後の維持・停止再開・個別会話の選択・コンソール・横方向のはみ出しを確認し、スクリーンショットを検査した。実モデルによる予定の遂行、通知、費用／モデル総予算は未検証／未実装。

## 最新: ユーザー指定の定期実行

- 固定間隔（1分以上）・初回UTC時刻・最大起動回数・仕事期限を管理DBへ保存。管理者GET/POST `/api/schedules`、PATCH `/api/schedules/:id`（enabled）を接続。Botには公開しない。
- 予定の発火記録と仕事を同じトランザクションで保存。作成要求のIDと入力ハッシュを照合し、再送・再起動の重複を防ぐ。過ぎた予定は1回へまとめ、次回は元の時刻の周期へ戻す。
- 全体停止・予定停止・休眠・アーカイブ・個別会話の認可を検査。前回の仕事が待機／実行中なら新規起動しない。予定停止は既存仕事を中止しない。
- 3回連続で完了しなければ理由付き停止。管理者の明示再開で失敗判定をやり直すが累計起動数は維持。上限に達した予定の再開は拒否。仕事ごとの既存24応答上限は変更しない。
- 117テスト・型チェック・ビルド成功。人工仕事・管理者HTTP・実DB再起動・書込失敗のロールバックを検証。実モデル・外部アカウント・配置は未実施。
- 残件: 管理画面、通知、曜日／タイムゾーンを含む時刻表、費用・予定単位のモデル呼出し総予算、変更検知と関心からの自発起動。既存予定の内容変更は停止後に新しいIDで作成する初期仕様。

更新: 2026-09-07。作業場所は `D:\niwa`。本番ルート `/home/niwa/niwa/` への配置は未実施。

## 過去手順の再利用（最新追加）

- Bot個別DBの第5移行で、手順・各版の本文・利用記録・重複防止記録を保存する。登録には同じBotの完了済み仕事が必要。適用条件・手順・出典と版を保存し、元の仕事の本文版も自動で出典へ加える。
- 共通ツール `procedure_save/search/read/restore/set_enabled/apply` を接続。更新は期待する版を照合し、過去版への復元も新しい版として保存する。無効な手順と古い版は採用しない。出典が変わった手順は返さず、再利用を拒否する。
- 採用は条件に合う理由と使用版を記録し、残作業の版照合と同じ個別DBトランザクションで手順を設定する。実際の外部操作は起動しない。読取時に利用した仕事の現在の状態と結果を返すため、計画メモだけで成功したとは扱わない。再送しても手順の版や利用件数を増やさない。
- 共有由来または現在の個別会話由来の手順だけを使用できる。個別会話で使った理由・結果は共有会話へ返さない。本文の更新・復元は登録元の会話から行う。記憶版変更では手順と過去版・利用記録を削除し、本文を含まない操作記録だけを残す。
- 112テスト・型チェック・ビルド成功。保存・競合・版復元・無効化・古い版の採用拒否、残作業との一括保存・同一操作再送、実際の仕事結果参照、Bot／個別会話の分離、再起動、出典変更・記憶削除後の拒否、モデルの共通ツール経路を人工データで検証した。
- 条件の意味的な適合判断はモデルが記録する。手順の自動改善、コード生成・コンパイル、管理画面での手順編集は未実装。

## 出典付きの仕事要約（最新追加）

- `task_summary_save` で結論・理由・未解決事項・次の手順と1〜20件の出所ID・版を受け取る。目的は現在の依頼から保存する。出所は会話・仕事・追加指示・成果物・自分の記憶で、保存前に `history_read` と同じ認可・版照合を行う。要約同士の循環参照は許可しない。
- 個別DBの第4移行で要約本体とFTS5索引を追加。仕事ごとに最新の要約を保持し、保存版・操作ID・入力ハッシュで同じ操作の再送を処理する。共有管理DBへ要約本文をコピーしない。
- `history_search` は自分の要約も検索し、共有由来または現在の個別会話由来のものだけを候補にする。`history_read(kind=summary)` で目的・出典・保存日時・現在の仕事状態とともに取得できる。生成された補助情報と明示し、承認や実行済み判定を置き換えない。
- 記憶訂正・削除・復元時の削除記録適用では、そのBotの要約と索引をトリガーで削除する。追加指示・回答でも対象の仕事の要約を削除する。会話や成果物等の出所変更・削除・認可変更は検索／読取時に再検査し、返す前に古い要約と索引を削除する（こちらは取得時の検査であり即時全件走査ではない）。
- 108テスト・型チェック・ビルド成功。Bot・個別会話の分離、再起動、版競合、出所訂正・削除後の除外と索引削除、モデルの原文取得→要約保存、バックアップ復元での削除済み記憶由来の要約・手順の復活防止を人工データで確認した。
- 自然文の内容はモデルが作る。出典が主張を意味的に裏付けることの自動判定、全仕事の自動要約、会話入力の要約への置換、過去手順の適用・版管理はまだ実装していない。

## 残りの手順の保存（最新追加）

- Bot個別DBの第3移行で `task_plans` を追加。現在の仕事の残りの手順を最大30件・各1,000文字・全体16,000文字まで保存し、共通ターンの `work_state.remaining_plan` へ版とともに渡す。未保存時は版0・空配列。
- `task_plan_update` は有効な担当Bot・リース・会話を検査し、期待する版が一致する場合だけ更新する。最後の操作IDと入力ハッシュも同じ個別DBのトランザクションへ保存するため、手順保存後・共通操作記録保存前に中断しても同じ操作を再送できる。
- 手順は補助メモであり、承認・実行済み・完了状態を変更しない。実行結果や子の状態は既存DBを引き続き正本とする。本文を共有管理DBの操作記録へコピーしない。
- 記憶版が変わるとトリガーでそのBotの手順を削除する。追加指示と質問への回答でも対象の仕事の手順を削除し、最新の依頼・回答から再構成する。削除後は版0へ戻る。
- 104テスト・型チェック・ビルド成功。旧個別DBの移行と履歴保持、再起動後の復元、競合と他Bot拒否、同一操作再送、訂正・指示・回答後の削除、次のモデル入力への接続を人工データで検証した。
- 出典・版を持つ自然文要約、別の仕事への手順の再利用はまだ未実装。

## 圧縮前のツール交換の再参照（最新追加）

- `task_history_read` は現在の仕事の0始まりの応答番号を受け取り、保存済みモデルイベントと対応するツール結果を返す。保存元のBot個別DBを読み、現在のBot・有効リース・会話認可・記憶版・破棄フラグを毎回確認する。他の仕事IDを指定する引数は設けない。
- ツール結果は操作番号と入力ハッシュが一致する内部・外部操作記録から読む。保存されていない結果はnullとし、成功と解釈しない。元のツールを再実行せず、検索や記憶読取のような都度取得する結果を固定コピーしない。
- 内容はJSONテキストを最大20,000文字ずつ返す。続きは本文・仕事・Bot・会話・記憶版を含むハッシュを必須にし、後から結果が保存されて内容が変わった場合も古い版を拒否する。再参照の結果自体は読取記録へキャッシュしない。
- 101テスト・型チェック・ビルド成功。実DB再起動をまたぐページ取得、他Bot拒否、結果更新時の版不一致、記憶訂正・追加指示・明示破棄後の取得拒否、共通ツール接続を人工データで確認した。
- 読みやすい自然文要約と、残作業の明示的な保存は未実装。

## 実行中の文脈圧縮（最新追加・初期版）

- 共通ターンの完了したツール交換の区切りで `fitContext` を実行する。システム指示・会話・現在の依頼・最新のDB作業状態は保持し、古いツール交換を組ごとに外して、収まる中で最大数の直近交換を残す。最後の交換は外さない。会話の40件切捨てを廃止した。
- 入力のUTF-8バイト数÷3（切上げ）+1024を概算トークン数とし、モデルの提示容量から出力予算を引いた範囲へ収める。1024はプロトコル等の見込み分。トークナイザーの計測値・実利用量ではなく、プロバイダー受入れを保証する値でもない。
- 容量不明時は圧縮せず、入力が128KiBを超えた場合は理由付きの確認待ちにする。この値はサイズ確認用であり推定容量ではない。容量既知でも保持必須部分が収まらなければ待機する。
- 圧縮後は新しいアダプターを取得し、プロバイダー固有の継続状態を捨てる。取得後に停止・記憶版を再検査し、新しい接続の容量で再判定する。保存履歴・ツール記録・24応答の上限・要求予算をリセットしない。
- 圧縮した本文キャッシュは作らず、再起動時は元の保存済み応答と操作記録、現在のDB作業状態から再構成する。新たな不変スナップショットへ私的本文を複製しない。
- 99テスト・型チェック・ビルド成功。長いツール結果の圧縮、45件より前の指示保持、ツール組保持、容量不明・圧縮不能の待機、圧縮中断後のDB再起動と読取重複防止を人工モデルで検証した。
- 会話・作業状態自体の圧縮はまだ行わない。残作業の明示保存、出所・版に追従する要約、取り除いた過去ツール交換の参照、実モデルの容量と受入れの検証は残件。

## コンテキスト容量の受け渡し（最新追加）

- サブスクのモデル一覧の `context_window` を選択プロフィールとアダプターへ渡す。アカウント共通の直列実行ラッパーも保持する。モデル接続を再作成したときは、その接続のプロフィールを使う。
- 値は正の安全な整数に限定。欠落は不明のままにし、`max_context_window`、出力予算、ターンの64,000トークン予算から推測しない。プロバイダーが提示した容量であって、Niwaの実測ではない。Ollamaのモデル仕様上限を稼働中の `num_ctx` と同一視しないため、Ollama側は引き続き容量不明。
- 人工モデル一覧からプロフィール・接続ラッパーまでの伝播、不明値維持、不正値拒否を検証。94テスト・型チェック・ビルド成功。圧縮判断への接続はまだ未実装。

## 保存状態からのターン再構成（最新追加）

- `Tasks.workState` は現在のBot・有効リース・会話認可を検査し、依頼と仕事状態、追加指示、同じ仕事の子の結果、決定等の報告、成果物参照、外部操作ID・入力ハッシュ・保存結果、保存済みモデル応答数を取得する。共通ターンはモデル呼出し前に毎回これを組み立てる。リーストークンや途中思考はモデル入力へ含めない。
- 外部操作の結果未保存は `unknown`、保存済みは `recorded` とし、記録済みエラーを成功とみなさない。表示した状態は認可・重複実行防止の代わりにはならず、実操作時の既存チェックを維持する。
- `artifact_create` は現在の仕事IDを成果物のできごとへ保存し、同じ仕事の再開時にID・名前・説明を参照できる。既存の仕事ID無し成果物を推定で関連付ける移行はしない。
- DBを閉じて再起動した後の再構成、他Bot・古いリース・停止後の拒否、別仕事の除外、実際のターン入力への接続を人工データで検証。93テスト・型チェック・ビルド成功。
- PLAN第20節の作業状態の初期版。残作業を明示する保存欄、出所と版を持つ要約、モデル実容量による圧縮・圧縮後の継続状態破棄は引き続き未実装。今回の取得処理は文脈量を減らしてはいない。

## 文脈保持の部品（最新追加・ターン未接続）

- Carried固定コミットの `retainCompleteExchanges` を抽出。初回・最新のユーザー指示、末尾の未応答指示、指定数の完全な交換を元順序で選ぶ。保存履歴は変更しない。
- ツール結果の順番は呼出し順と異なってもID・名前が一致すれば受け入れる。孤立・重複・欠落・名前不一致は、捨てる予定の古い部分にあっても拒否する。保持数0の扱いと不正値検査を追加。
- 92テスト、型チェック、ビルド成功。交換保持・入力不変・壊れた組の拒否を人工履歴で検証。まだ実行中の文脈圧縮機能としては未完成。モデル実容量、DB作業状態、圧縮判定、訂正時の再構成、プロバイダー継続状態破棄への接続が必要。
- 出典と差分を `provenance/carried-compaction.json`、ライセンスを `THIRD_PARTY_NOTICES.md` と `licenses/Carried-Apache-2.0.txt` に保存。

## 出所本文の取得（最新追加）

- `history_read` を共通ツールへ追加。会話・仕事・追加指示・成果物・自分の記憶について、検索の出所IDから保存元の本文を取得する。検索索引の本文を正本として返さない。取得ごとに現在のBotと会話の公開範囲を確認する。
- 本文は最大20,000文字ずつ取得し、続きの位置と内容・出所・記憶版から作るハッシュを返す。2回目以降はその版を指定し、内容変更時は先頭からの読み直しを要求する。削除済みの記憶や、別の個別会話の本文は返さない。結果を読取記録へ固定コピーせず、その都度現在の保存状態を参照する。
- 検証: `npm run check` 成功（90テスト）。長い成果物を分割取得して原文と一致すること、続きでの版指定、記憶訂正後の古い版拒否、削除後の参照拒否、個別／共有の境界、不正な種類と位置の拒否を人工データで確認した。
- 文脈圧縮・作業状態の再構成・自然文要約は次の作業であり、本文取得の追加だけで長い仕事の継続が完成したとは扱わない。

## 認可付きの過去情報検索（最新追加）

- `history_search` をモデルの共通ツールへ追加した。過去の発言、仕事の依頼／結果、追加指示、成果物と自分の記憶を文字列検索する。結果には種類・出所ID・ルームID・最大1,000文字の抜粋・省略の有無を付け、記憶には版と元メッセージIDも付ける。
- 検索先は、現在のBotが読める共有会話と現在の個別会話に限定する。同じBotが参加する別の個別会話も自動投入しない。他Botの記憶DBは開かない。認可を適用してから管理DBの情報を最大20件、自分の記憶を最大20件取得する。
- 管理DBにFTS5のtrigram索引を追加し、既存の保存データを移行時に索引へ登録する。発言・仕事・追加指示・成果物の追加／変更／削除に追随する。個別記憶は既存のBot専用索引を使う。3文字以上は引用したリテラルのFTS検索、短い文字列は部分一致で日本語にも対応する。外部サービスや埋め込みモデルは使用しない。
- 検証: `npm run check` 成功（89テスト）。共有／個別／別個別／他Botの境界、全種類の出所、記憶の訂正削除、再起動、日本語1文字と記号を含む検索、旧DBの既存データからの索引作成、仕事完了結果の索引更新を人工データで確認した。
- 残件: 検索結果からの本文取得、作業状態の再構成、文脈圧縮、自然文要約と派生情報の無効化、管理画面の横断検索。現在の検索はモデル向けの文字列検索であり、長い仕事の継続処理が完成したという宣言ではない。

## 共有ファイルのBot書込接続（最新追加）

- `workspace_write` を共通ターンへ追加した。専用実行サービスが設定され、現在の会話が共有の場合だけモデルへ公開する。個別会話で呼び出しても拒否し、私的資料は従来の会話内成果物として扱う。ツール引数は相対パス・本文・期待する版に限定し、実行IDは本体が発行する。共有会話へ移さず個別会話から明示的に共有する操作は未実装。
- 本体の新しい `external_operations` テーブルに仕事・操作・入力ハッシュ・実行ID・結果を保存する。本文を操作記録へ複製せず、実行サービスへ渡す前に記録する。同じ操作の再開には同じ実行IDを使い、別入力での使い回しを拒否する。再送先は専用サービスの永続記録で重複実行を抑止する。既存の読取用 `readOnce` は書込に使わない。
- 結果不明／通信失敗時は確認待ちに入り、自動で次のモデル処理を続けない。管理者が再開すると同じ実行IDで専用サービスの記録を照合する。停止や中止の後で完了応答が届いた場合は結果だけ記録し、仕事を再開しない。既に実行サービスへ渡した書込が停止後に完了する可能性はある。
- 書込クライアントも固定IPCを使用し、返却パス・版と送信した本文のハッシュを照合する。専用サービスが返す既知の失敗コードと、確定した成功を区別する。本体から共有ファイルを直接開く経路は追加していない。
- 検証: `npm run check` 成功（86テスト）。IPCでの操作IDの維持と後続編集の非上書き、本体再起動後の実行ID維持、他Botによる呼出し拒否、停止後の結果保存と非再開、個別会話での非公開／拒否、共有会話の人工モデルによる書込完了を確認した。実Linuxの別OS主体・権限・隔離は未検証。
- 残件: 共有ファイルの管理画面への接続、結果不明の解決UI、実行サービスの記録が欠けたバックアップ復元の扱い、生成プログラムとの競合、実モデルでの調査完遂。本体DB・実行サービス記録・共有ファイルを別々の世代へ戻しても安全に自動再開できるとはまだ扱わない。

## 共有ファイルの書込記録（最新追加・Bot接続前）

- 実行サービス側の `WorkspaceWriteLog` に、操作ID・入力ハッシュ・結果を保存する専用SQLiteを追加した。書込前に操作を記録し、同期保存を設定する。ファイル本文は記録へ複製しない。完了結果に共有パスと版を残す。
- 同じ操作ID・同じ入力の再送は保存済みの結果を返し、後から編集されたファイルを上書きしない。同じIDに別の入力を渡すと拒否する。中断により結果が未保存の場合は現在のファイルを読み、予定した内容のハッシュと一致するときだけ完了記録を補う。一致しない／読めない場合は `outcome_unknown` を返し、再書込を行わない。版の競合等、明確な失敗も保存して同じ操作を繰り返さない。
- 専用サービスの書込RPCは `operation_id` を必須にした。記録部品なしのサーバーは書込を受け付けない。新しい起動形は `npm run workspace -- --workspace <共有ルート> --socket <私的ソケット> --state <実行主体専用ディレクトリ>`。共有領域内のソケット・実行状態を拒否し、状態ディレクトリは実行主体の所有・他ユーザー／グループの権限なしを要求する。専用状態のプロセスロックで二重起動を防ぐ。実際のLinux配置は行っていない。
- 検証: `npm run check` 成功（82テスト）。再起動後の完了結果再利用、後続編集の保持、書込後に応答を失った場合の照合、未実行／結果不明の保留、入力変更の拒否、記録部品が閉じている場合の未実行を人工ファイルで確認した。テスト終了時のDBクローズ順を修正し、全テストが終了することも確認した。
- 残件: 本体からの書込クライアントと操作記録、停止後の結果回収、Bot向けの共有範囲の認可、結果不明状態の画面表示と解決手順、バックアップ復元時の操作記録との整合。今回の記録追加だけで外部操作全体や本番隔離が完成したとは扱わない。

## 共有ファイルの読取接続（最新追加）

- 本体は `src/tools/files/client.ts` のIPCクライアントから専用サービスへ接続する。モデルへは `workspace_list`／`workspace_read` を公開する。管理DBを扱うプロセスは、Bot指定の共有ファイルパスを開かない。
- 有効化は管理者の製品設定 `config/niwa.json` の任意整数 `workspaceExecutorUid` に、別のLinux実行主体のUIDを指定する。設定なしではツールを公開しない。接続先は製品ルートの `runtime/sockets/workspace.sock` に固定し、モデルや設定から自由な通信先を指定しない。呼出しごとに、ソケットがそのUIDに属し、親ディレクトリとソケットに他ユーザー向け権限がなく、リンクでないことを検査する。Linux以外・rootのUID・アプリ自身のUIDは設定先として拒否する。今回この設定を実稼働環境へ書き込んでいない。
- 通信は10秒・応答512KiB以内。応答のスキーマ、要求と同じ相対パス、本文64KiB以内、本文とSHA-256の一致を検証する。UTF-8 BOMは本文に保持して版照合を一致させる。任意の追加フィールドや不正な一覧名を受け付けない。
- 読み取りは仕事の既存読取記録へ保存し、再開時に同じ操作結果を再利用する。呼出し前後に仕事の実行主体・停止・記憶の版を確認し、停止や記憶訂正後に戻った資料は保存・後続応答に使わない。
- 検証: `npm run check` 成功（79テスト）。人工ファイルを使うローカルIPCで読取・一覧・BOM・不正パス応答・本文改変・過大応答・中断を確認し、人工モデルの共通ターンで共有資料の利用とBot境界・停止後の破棄を確認した。Windowsでは名前付きパイプを試験に使用し、本番のUnixソケット権限やOS分離を検証したとは扱わない。
- 残件: 書込ツールの永続的な実行記録、結果不明時の照合、共有を許可する会話の範囲、管理画面接続、Linuxでの別主体・OS隔離・競合の試験。書込は専用サービス部品にあるが、まだBotへ公開していない。

## 共有ファイルサービスの部品（最新追加・接続前）

- `src/tools/files/workspace.ts` にUTF-8テキストの読取・一覧・更新を追加した。共有ルートからの相対パスだけを受け付け、越境・絶対パス・Windowsの特殊名／代替ストリーム・シンボリックリンク・複数ハードリンクを拒否する。本文64KiB、パス512文字／16階層、一覧200件の上限を持つ。
- 読取時のSHA-256を版として更新時に照合する。古い版では上書きせず、作成時は未存在を指定する。書込は同じディレクトリの一時ファイルから置換し、内容が既に一致する再試行は変更せず成功を返す。別の内容へ変更済みなら競合になる。単一サービス内では書込処理を直列に行う。
- `src/tools/files/server.ts` と `src/entrypoints/workspace.ts` に専用サービス入口を追加。コマンドはビルド後の `npm run workspace -- --workspace <絶対パス> --socket <絶対パス>`。CLIはLinux・非rootを要求し、他ユーザーが通れないディレクトリのUnixソケットだけで待ち受ける。既存ソケットは自動削除しない。今回サービスの本番起動・権限設定は行っていない。
- ARCHITECTUREの指定に従い、この部品は管理DBを読めない `niwa-exec` 側に置く。本体サービスからBot指定パスを直接開く接続は追加していない。`niwa`と別主体であること、読取可能範囲、ソケット所有者／グループ、rootless Podman等のOS境界は配置時の検証が必要。NodeのPermission Modelやパス検査だけで隔離済みとは扱わない。
- 次の残件: サービス間クライアント、永続的な書込実行記録、結果不明時の照合と停止／再開、会話の共有範囲の認可、Botツール・管理画面への接続。生成プログラムが直接共有フォルダを編集する場合の競合・リンク差し替えを含むOS境界試験も残る。共有ファイルがBotから使用可能になったという宣言ではない。
- 検証: 74テスト・型チェック・ビルド成功。人工の非共有ファイルを使い、越境・ディレクトリリンク・ハードリンク・旧版編集の拒否、同内容再試行、テキストと出力の制限、RPC要求とエラーの制限を確認した。HTTP部品の試験は人工ファイル用のループバック接続で行い、実LinuxソケットやOS分離の証明には使っていない。

## Web検索（最新追加）

- `web_search` を共通ターンへ接続。Braveの固定APIへクエリを送信し、最大5件のタイトル・URL・抜粋を構造化した未信頼の資料として返す。400文字のクエリ制限、応答256KiB・15秒制限、転送拒否、外部エラーとキーの除外を行う。原文確認は `web_read` へ分ける。
- サービス起動時、製品ルートの `secrets/brave-search-key` だけを読む。ファイルがなければ検索ツールをモデルへ公開しない。設定する場合は管理者が自身の利用可能なBraveキーを同ファイルへ保存し、Linuxでは所有者だけ読める権限にする。変更にはサービス再起動が必要。コード・チャット・通常設定・バックアップへキーを置かない。環境変数や他アプリのキーは探索しない。アカウント作成・契約・実キーによる検索は今回行っていない。
- 設定画面からの登録・解除と検索接続状態表示は未実装。実Brave応答、実モデルによる調査完遂は未検証。検索サービスの利用可能な接続が必要な点を残件として保持する。
- `npm run check` 成功（70テスト）。固定URL・認証ヘッダー・応答制限・秘密の除去・キーなし時の非公開と、1体のBotが検索→原文取得→出典付き成果物保存を行う共通ターン処理を人工データで検証した。公開ページの実HTTP取得は下記の通り別途確認した。
- Staticeの固定コミットを参考にNiwaへ再実装した。参照先と変更点は `provenance/statice-web.json`。検索方式の契約は[Brave公式文書](https://api-dashboard.search.brave.com/documentation/services/web-search)で確認した。TavilyやStaticeの秘密管理・旧ランタイムは取り込まない。

## 公開ページ取得（最新追加）

- Bot向け `web_read` を非同期の共通ツール処理へ接続。公開IPv4、標準HTTP/HTTPS、成功したHTML・プレーンテキスト・JSONを対象とする。HTMLは実行せず、そのまま未信頼の資料として返す。最終URL・取得日時・省略の有無を付ける。
- DNSの全IPv4回答を検査し、検査したIPを接続に固定する。認証情報入りURL、私的・予約IP、非標準ポート、IPv6、HTTPSからHTTPへの転送を拒否。転送先は毎回検査し最大5回、全体15秒、本文256KiB・モデル出力20,000文字を上限とする。Cookie、認証ヘッダー、プロキシ設定を引き継がない。
- 読み取り結果は仕事と操作IDに紐づけて再利用する。非同期通信中はDBトランザクションを保持せず、保存時に実行権限を再確認する。停止・記憶訂正中に戻った結果を保存・後続操作へ利用しない。未保存の読み取りはクラッシュ後に繰り返す場合がある。外部書き込みにはこの仕組みを使用しない。
- 検証: `npm run check` 成功（66テスト）。不正URL、私的／予約IP、混合DNS回答、転送先、本文制限、DNS待機中断、Bot境界、再利用、停止と訂正を人工入力で確認。実装した通信経路で認証不要の `https://example.com/` を取得（HTML 559文字）できた。実モデルを通した調査や本番ネットワークの隔離は未検証。
- Statice固定コミットの検索・ホスト検査テストを再確認した。旧テストが許可するTEST-NET等はNiwaでは拒否し、既定OFFの内部ホスト制限も継承しない。今回のページ取得コードはNiwaで新設。検索APIキーの設定・Web検索・共有ファイル操作は次の作業。

## 今回動く範囲

TypeScriptの内部サービス、モデル接続部品、モデル向け内部ツール、親子タスクとスケジューラーを実装した。
管理者向けHTTP API、セッション認証、製品ルートを使う起動部とOllama設定を追加した。承認済みの `prototype/` を残し、本体用 `web/` に画面・CSS・アイコンを引き継いでAPIへ接続した。サブスクモデルの設定統合は未実装。

- Node.js 24.16.0、組み込みSQLite、TypeScript。管理DBとBot別の個別記憶DBを作成する。
- 初回のリーダー1体を冪等に登録。名前は仮の「リーダー」で、過去の人格は入れない。
- リーダー限定のBot生成、休眠・再招集。休眠を含め生成Bot上限10体、リーダー別枠。
- Astra low／Luna maxの希望設定を保存。モデルID・能力・実際の利用可否はカタログ接続時に検証する。
- 共有／個別会話と発言を保存。全Botは共有会話を読める。個別会話は参加Botと管理者だけが読める。
- 実行主体は信頼するサーバーが発行する不透明なオブジェクト。引数で他BotのIDを指定しても他Botの記憶へアクセスできない。
- 個別記憶を保存・検索。出所メッセージを実際に閲覧できることを確認し、出所ルームはDBから決定する。
- 管理者による記憶訂正・削除、楽観ロック、本文を含まない操作履歴。検索索引も同じトランザクションで更新する。
- 会話用の記憶選択では、共有会話の記憶と、現在と同じ個別会話に由来する記憶だけを返す。
- 記憶変更の世代番号を返し、生成中の文脈が古くなったかを判定できる。
- 停止・人数上限・同時実行数・バックアップ日数を保存。手動停止中はBotの発言・生成・記憶追加を拒否する。

## 接続・タスク処理の追加

- 固定CarriedコミットからOAuth、Responses、ツール呼び出し復元、モデル一覧などを取り込み、出典と原本ハッシュを `provenance/carried.json` に記録。所有者の指示により `licenses/` の別コピーは生成しない。
- 認証は明示された保存先のみを使用し、アカウント単位のrefreshを共有。ログアウト後の遅い応答で資格情報を復活させない。実アカウントへのログインは行っていない。
- Codex接続の並列セッション能力はfalseを維持。`CodexConnection` はタスクごとに独立アダプターを生成し、アカウント単位のゲートでモデル要求を直列化する。待機中のキャンセル・時間切れも次の要求を追い越させない。入力検証、ストリーム解析、制限時間、中断、秘密の秘匿化を人工応答で検証。
- Ollamaの導入済みモデル一覧、モデル別能力、会話・ツール応答の変換を実装。クラウド転送モデルを拒否し、モデルの自動ダウンロードは行わない。
- Ollama URLとBotごとの選択モデルを管理DBへ保存。`ModelGateway` が導入済みモデルと能力を確認してから選択を保存し、起動部がこの設定から実行アダプターを作る。確認中にURLが変わった場合は保存しない。設定変更では古い実行権を無効化する。
- 永続タスクは親子依頼、待機、管理者の返信、完了・失敗・期限・キャンセルを扱う。待機中の親は実行枠を占有しない。古い実行権は拒否する。
- 共通ターン処理はモデルが要求したBot生成・休眠・再招集・依頼・質問・本人の記憶操作を内部ツールへ接続。ツールの操作記録で再実行時の重複を防ぐ。
- モデル応答の再開用記録は当該Botの個別記憶DBに保存。管理DBの操作引数はハッシュのみ。訂正・削除で文脈が古くなった応答とそれ以前の生成履歴を破棄し、接続側の継続状態も再作成する。文脈世代は訂正・削除の無効化番号であり、新規記憶追加のたびには変更しない。
- スケジューラーは同じBotの処理を直列化し、設定した同時実行数を守る。停止・休眠・キャンセル・期限切れで無効になった通信を中断し、終了時には保有中の仕事だけをキューへ戻す。起動部は専用SQLite排他ロックを取得してから復旧し、HTTP待受後にスケジューラーを開始する。別プロセスの強制終了後にロックが解放されることを検証済み。systemd登録は未実施。

## この段階で決めたこと

- 製品ルートは `app/config/state/secrets/workspace/runtime/backups/logs` に分離する。`src/config/paths.ts` が明示した絶対ルートから保存先を決め、初期化する。現在の `D:\niwa` は `app/source/` に相当するソースリポジトリ。Linux本番のパス作成・所有者設定は未実施。
- `src/web/api.ts` は会話、投稿、設定、休眠、記憶の訂正・削除、タスク再開・キャンセル、イベント取得の管理者APIを提供。モデルから任意の主体を指定する入口はない。
- 管理画面は初回起動時に `secrets/admin-key` へ作るランダムな管理キーでログインし、8時間のHttpOnly・SameSite=Strictセッションを使う。HTTPSではSecure属性を付け、HTTPはループバック開発用だけを許す。固定Origin・Hostを検証し、更新にはOriginを必須とする。キーは再起動時に維持し、標準出力へ書かない。画面ログイン・本番TLSは今後の統合作業。
- ブラウザーの投稿IDと内容ハッシュで再送を検出し、投稿とBotへの仕事を同じトランザクションで保存する。失敗時は両方ロールバックする。

- DB上の1ルームをUIの1スレッドとして扱う。共有ルームは複数作成でき、すべて全Botへ公開する。
- 個別会話のBot参加者は作成時に固定する。ユーザーは全個別会話を閲覧できる。
- Botが個別会話を作る場合は本人の参加を必須にする。参加者の追加による過去ログ公開は現段階では提供しない。
- 記憶の監査履歴は操作種別・主体・対象ID・版・時刻のみを残す。過去本文を残す差分履歴は提供しない。
- 個別記憶DBは最初に必要になった時に作る。ディレクトリ名には登録済みのサーバー発行UUIDだけを使う。

## 検証

ルートで実行する。UIの依存・ビルドは従来どおり `prototype/` 側に分かれている。

```powershell
npm ci
npm run check
```

`check` は型チェック、58件のテスト、バックエンドとWebのビルドを順に実行する。すべて成功済み。
テストは一時ディレクトリに人工データだけを作り、終了時に削除する。
管理者／Botの偽装、他Bot記憶・個別会話への越境、共有文脈への私的記憶混入、訂正・削除、
上限、休眠・再招集、再起動時の記憶・会話・停止の保持を検証する。
WindowsのNode.js 24.16.0で検証。Linux側の同等性は未検証。

## 本体UIの接続

- `web/` は承認済みReact画面の本体用コピー。20画像、5テーマ、既存CSSの密度・カラム・スクロールを維持する。プロトタイプのSitesテンプレートは変更していない。
- 管理キーでログイン後、保存された会話、メンバー、仕事、個別記憶を表示する。投稿、個別会話、停止・再開、プロフィール・見た目、記憶訂正・削除、人数・同時実行数・バックアップ日数・Ollama URLをAPIで保存する。テーマと選択中の会話IDだけをブラウザーへ保存する。
- メンバー追加はリーダーへの依頼として会話に保存する。画面側でBotを直接作らない。仕事の詳細から接続待ち・回答待ちを再開できる。
- プロフィールは管理DBで保存し、表示上の「役割」を変えても実行権限は変わらない。本人のターン文脈へ人格設定を含める。Botの `profile_update` は本人の名前・性格だけを保存できる。初回の質問を会話へ投稿し、同じ会話でそのBotへの回答待ちが1件ならユーザーの返信で再開する。質問→返信→人格保存→次の生成への反映を人工モデルで確認した。
- 初期人工Bot・サンプル会話・サンプル記憶は本体画面へ持ち込まない。サブスク設定、自動切替、自律起動、共通ルール編集、バックアップ実行は準備中表示または無効なコントロールにし、添付・外部公開を偽って成功させない。
- `dist/client/` のビルドのみを静的配信する。製品ルート・秘密・共有作業場は静的公開しない。Viteは依存監査の修正パッチ6.4.3を採用し、追加時の監査は脆弱性0件。
- Browserプラグインがないため、既存Playwrightとインストール済みChromeでローカル人工データを検証。1920×1080と390×844で、ログイン→会話作成→投稿→再読み込み、プロフィール保持、テーマ保持、モバイル会話を確認。空白・エラーオーバーレイ・横はみ出し・最終console error/warnはなし。
- 新規会話は会話・最初の発言・仕事を同じトランザクションで保存する。リクエストIDと内容ハッシュを保存し、同じ内容の再送は元の結果を返す。宛先が不正なら会話自体も作らない。
- 残るUI課題: 初期対話の実モデル検証、Bot恒久削除、休眠管理の入口、履歴ページング、訂正履歴の取得、サブスク認証とフォールバック、バックアップ等。現状のポーリングは全ルーム・全個別記憶を取得するため、データ量が増える前に取得範囲を絞る。

## 日常利用の5機能（2026-09-07追加）

- 更新されたプロトタイプの活動画面、成果物表示、仕事詳細、会話の整理操作を本体へ移した。初期画面は「活動→できごと」。既存の4つのナビゲーションと文字サイズは維持している。
- できごとはBotの完了、質問、`decision_report` による決定の報告から保存する。確認済みも管理DBへ保存する。過去の仕事を推測してサンプル報告を補充しない。
- `artifact_create` は現在の会話の範囲でテキスト資料を保存する。作成者・種類・名前で絞り込み、本文プレビューとその本文のダウンロードを提供する。本文はHTMLとして実行しない。現段階の保管先は管理DBであり、画像・バイナリ添付や共有作業ディレクトリとの同期は未実装。
- 仕事の一時停止・再開・中止・再試行・追加指示・管理者による完了をAPIへ接続した。個別停止は再起動後も維持し、全体停止や休眠を迂回しない。実行中の追加指示では古い実行権を失効させ、応答計画を破棄して保存済み指示を次の生成へ渡す。親が既に続行した子の再試行は拒否し、新しい依頼を必要とする。実行回数の区切りに到達した仕事も新しい依頼が必要。
- スレッドのピン留め・アーカイブ・復帰は管理DBへ保存する。アーカイブは会話を削除せず、実行中の仕事も中止しない。新規返信は復帰後に受け付ける。
- アイコンの「状態に合わせる」は実行中に揺れ、画面が検出した完了時に一度弾む。休眠・全体停止・端末の動きを減らす設定では静止する。
- 追加のブラウザー確認で、確認済み保持、資料の検索・本文・ダウンロード内容、仕事操作・追加指示、ピン留め・アーカイブ・復帰、PC／モバイルの横はみ出しを確認。console error/warnは0件。実モデル・実スマートフォンは未検証。

## 起動部

ビルド後、`npm start -- --init --root <製品ルートの絶対パス> --origin <ブラウザーから使うOrigin> --port 3210` で配置設定を新規作成する。既存設定は上書きしない。以後は `npm start -- --root <同じ製品ルート>`。HTTP待受は常に127.0.0.1で、本番は前段のHTTPSプロキシを想定する。これは未配備のコマンド仕様であり、Linuxのパス作成やサービス登録を済ませたという意味ではない。

Ollamaは認証済みの `/api/model-settings` でURLを設定し、`/api/models` で一覧取得、`/api/agents/:id/model` で選択できる。CLIはその保存設定で実行する。未設定のモデルやサブスク接続は `waiting_provider` になる。サービス結合テストではローカルの人工Ollama HTTPサーバーを使い、既定の接続経路で会話・ツール呼び出し・回答保存・設定の再起動保持を確認した。実際のモデルを検証済みとは扱わない。

## 未実装と境界

- `administrator()` と `agentSession()` は信頼するアプリ起動部だけが呼ぶ内部関数。ログイン機能ではない。
  HTTPから渡されたロールやBot IDをそのままこの関数へ流さない。Runtimeオブジェクトを生成コードへ渡さない。
- 管理者向けの全記憶検索を、そのまま会話中のBotツールへ公開しない。モデル接続時には会話の出所範囲を固定した窓口を作る。
- 人格の版管理、Bot恒久削除は未実装。会話の分割取得は実装済み（上記最新版）。記憶の分割取得と訂正履歴の画面表示は実装済み（上記最新版）。
- バックアップの保存・圧縮・世代整理と、新しい製品ルートへの復元コマンドは実装済み。既存ルートへの切替と管理画面の復元操作は未実装。
- 記憶削除は稼働DBの論理検索結果から除外する。WAL・FTS内部ページ・過去バックアップを含む物理消去の保証ではない。
- 文脈世代による応答の破棄／再生成は人工モデルで検証済み。実接続での検証は未実施。
- ファイルシステム・Podman・ネットワークの隔離、イベントのプッシュ配信、外部操作は未実装。Web認証・HTTP API・接続UIは人工データのローカル試験のみ。
  内部APIの認可テスト合格をOS隔離の証明にしない。
- WSLの読み取り確認は `Wsl/EnumerateDistros/Service/E_ACCESSDENIED`。Ubuntuの状態を取得できていない。
- Carriedの選定部品は取り込み済み。Statice／Cliveはまだコピーしていない。固定コミットと取り込み方針は `CODE_REUSE.md` を維持する。

## 次の実装

サブスク接続を本体へ統合した。`Subscription` がOAuth開始・中止・解除を管理し、`secrets/codex.json` だけを認証情報の保存先にする。既存のCodex設定や端末の資格情報は探索しない。設定画面の明示した接続操作でのみOAuthを開始し、画面へは認証URLと限定した状態だけを返す。実アカウント操作は実施していない。ブラウザー認証のローカルコールバックは[公式認証資料](https://learn.chatgpt.com/docs/auth)を参照し、現状は同じPCのブラウザーからの接続を案内する。遠隔端末向けデバイス認証は未実装。

メンバー画面からCodexのモデル一覧・推論強度を取得し、サーバーで一覧にある組合せを検証して保存する。`ModelGateway` はサブスク用の共通接続から毎回独立したアダプターを作る。アカウント変更・解除は古い仕事の実行権を失効させ、取得途中にアカウントが変わったモデル選択も拒否する。並行セッション不可の扱いを維持する。モデル一覧は選択情報であり、実モデルのツール能力を実証したものではない。初期モデルIDと推論強度も実アカウントでの照合は残る。接続前から待機中の仕事は仕事画面で再開する。

人工OAuth応答による開始・保存・中止・解除と状態に秘密が含まれないこと、人工カタログによる不正な選択の拒否、サブスク経路の人工応答と解除時の失効を検証した。ブラウザーでは認証とカタログを人工応答へ差し替えて開始・中止・モデル／推論強度の送信内容を確認した。実認証URLを開いたり実モデルを呼んだりしていない。PC／モバイル表示、既存の会話・プロフィール・テーマ保存も確認済み。利用枠分類・Ollamaへのフォールバックと回復時の復帰は未実装。

復元コマンドを追加した。元のサービスを停止した状態で `npm run restore -- --root <現在の製品ルート> --backup <バックアップID> --to <まだ存在しない新しい製品ルート>` を使用する。現在の削除記録が必要なため、元のstateが失われた状況の災害復旧はまだ扱わない。ソースのプロセスロックを保持してバックアップを検証し、SHA-256・ファイル数・許可したパス・SQLite整合性を確認する。展開は1DBにつき4GiBまで。既存の保存先へ上書きしない。

削除記録を新しい管理DBへ引き継ぎ、該当記憶を索引からも削除し、古いモデル応答記録を破棄する。バックアップには存在しなかったBotや記憶の削除IDも残すため、さらに古いバックアップからの復元や同じ操作の再送で復活させない。復元先の活動は一時停止にし、秘密ファイルを移さず、起動時に新しい管理キーを作る。元の製品ルートは維持する。Linuxでのサービス切替・画面からの復元要求・外部操作の実行記録を復元後に照合する部分は残る。

追加試験は、後から削除した記憶の再適用、バックアップ後に作って削除した記憶の再送拒否、チェックサム不一致、パス越境、既存保存先の保護、稼働中ソースの拒否、新しい管理キー、停止状態の保持を確認した。

バックアップ保存の追加: `Runtime.snapshot` が同期区間で管理DBと全Botの個別DBを `VACUUM INTO` で取得する。取得後は通常活動へ戻り、`Backups` が非同期でgzip圧縮し、ハッシュ・設定・ファイル一覧のmanifestを完成後に保存先へ移す。通常バックアップにはsecretsやブラウザーセッション、共有作業フォルダーを含めない。現在テキスト成果物は管理DB内なので保存される。初回起動時と前回保存から24時間後に自動実行し、設定画面から手動保存もできる。保存日数は既存設定を使用する。時刻指定・自動保存の無効化は未接続。失敗時は未完成の一時保存を消し、成功履歴へ出さない。

人工データで、全DBの同時点保存、保存後も活動を続けられること、圧縮したDBの読み戻し、資格情報の除外、期限切れの整理、失敗時の後始末を確認。起動時の自動保存・画面からの手動保存と日時・件数表示を1920×1080／390×844で確認し、横はみ出しとconsole error/warnはなし。これは製品への復元・削除記録再適用の試験ではない。Linuxでの別主体・保存権限や電源断時の耐久性は未検証。

サブスクの認証・モデル能力確認・利用枠の分類とフォールバック、初回人格対話、残るUI操作と取得範囲を実装する。
アカウント操作、外部公開、本番サービス起動は行っていない。
# 利用上限の分類（2026-09-07 追加）

続く追加: `model_settings.fallback_model` を移行で追加し、`PUT /api/model-settings/fallback` から指定・解除できる。カタログ掲載、ローカル実行、ツール対応を検証して保存。検証中のURL変更を拒否し、保存後にURLを変えた場合は選択を解除する。Botの通常モデルは変更しない。再起動後の保持、能力不足・リモート・未インストール・越権・設定競合の拒否を人工データで確認。`npm run check` 成功、61件。画面からの選択、自動切替・復帰はまだ未実装。

最新PLAN第20節により、次の着手は外部ツール→検索・継続→手順再利用→定期・自発実行を優先する。上限時切替の残件は取り消さず保持する。

サブスク接続のHTTP 429／失敗ストリームにある明示的な `usage_limit_reached` を `QUOTA_EXCEEDED` として共通契約へ渡す。一般の429、文面だけに上限を含む応答、認証・権限・サーバーエラーは切替対象へ分類しない。`resets_at` は有効なUnix秒だけ `reset_at` に保持する。回復時刻は予定であり、回復済みの証拠にはしない。自動再送は行わず、現状のターン処理は接続待ちになる。

参照: [Codexのエラー契約](https://github.com/openai/codex/blob/main/codex-rs/protocol/src/error.rs)、[Responsesのエラー構造](https://github.com/openai/codex/blob/main/codex-rs/codex-api/src/sse/responses.rs)（2026-09-07確認）。Niwa側の保守的な分類を新設し、上流の実装全体は移植していない。

人工応答で通常429との区別、不正な回復時刻の除外、部分出力がある失敗を完了にしないことを確認。`npm run check` 成功、テスト60件。以下の過去の58件表記はこの追加以前の結果。指定Ollama設定、アカウント共通の上限状態、自動切替、回復確認と復帰、画面への切替理由表示は未実装。実アカウントの上限応答は未検証。

## 表示修正: 長い状態文の見切れ（2026-09-07）

web/src/styles.css と prototype/src/styles.css の status-label を1行固定から枠内の折り返しへ変更。max-width/min-width、overflow-wrap、行間とドットのベースライン配置を指定した。メンバー一覧、会話の右側、スマートフォンのメンバーの様子に適用。既存の小画面メンバー一覧で状態文を非表示にする仕様は維持。
本体npm run build成功。Browser plugin not availableのためPlaywrightで127.0.0.1:4173のプロトタイプと本体CSSを差し替えた同一DOMを検証。1920/1366/390pxで長い日本語と連続英数字の折り返し・横はみ出しなし、メンバー切替、モバイルの様子ダイアログ、画面内容・URL/title・Vite overlayなし・console errorなしを確認した。本体API経由の実データ表示と実端末は今回未検証。人工文をDOMに注入して長文を再現し、本番データは変更していない。

2026-09-07 状態文の追加指定: 共通StatusLabelで最初の「 · 」を区切りとしてステータス名と説明を分離し、「再試行待ち：」の次行に説明を0.5em字下げして表示する。説明なしの休眠・停止等は1行。web/prototype両方のcomponents.jsxとstyles.cssへ適用。本体ビルド成功、Playwrightで1920/1366/390pxのプロトタイプを確認（メンバー切替、右側・モバイルダイアログ、改行・全文・横はみ出しなし、pageerrorなし）。本体API経由の表示は未検証。
2026-09-07 追加調整: ユーザー指定により状態説明の0.5em字下げを撤去し、ステータス名と左端を揃えた。デスクトップの右側「メンバーの様子」を240pxから300pxへ拡張。web/prototype両方のCSSへ適用。本体ビルド成功。Playwrightで1920/1366/390pxのメンバー切替・様子表示・横はみ出しなし、説明の左端一致とPC欄幅300pxを確認。

2026-09-07 吹き出し表示: ユーザー提示の参考画像をもとに会話本文・引用・添付をmessage-bubbleへまとめ、名前・時刻・スレッド題名は外側に配置。淡いテーマsurface色、丸角、左下だけ小さな角、本文幅に合わせたサイズにし、アイコンを左下へ配置。旧返信の縦線は撤去。web/prototypeのsrc/pages/Conversation.jsxとsrc/styles.cssへ反映。本体ビルド成功。Browser plugin not availableのためPlaywrightでデモの1920/1366/390px、送信・引用返信・改行長文・長いURL・横はみ出しなし・pageerror/console errorなしを確認。5テーマの背景切替も確認。本体API・添付ファイル操作の再試験は未実施。

## 2026-09-07: 上下スクロール矢印と本体機能のデモ追加

ユーザーのスクロールバー表示指定: メンバー一覧は棒とトラックを隠し、はみ出すときだけ上下のボタンを表示。端に到達した方向は無効化。MemberScroll.jsxでResizeObserver・scrollイベントに追従し、ホイール・キーボード・タッチはネイティブスクロールを維持。小画面の横並び一覧は既存動作を維持。web/prototype両方のsrc/MemberScroll.jsx（新規）、src/pages/Members.jsx、src/styles.cssに適用。ネイティブ疑似要素の矢印案は動作確認できなかったため撤去し、実ボタン方式に置換済み。

ユーザーの「本体web/srcにありデモにない機能を追加」依頼に対し、次をprototypeだけへ追加:
- 活動→定期実行: 本体のSchedules.jsx/schedules.cssをベースに、3件のサンプル（有効・停止・回数到達）、予定追加、会話/担当Bot選択、初回日時・間隔・回数・期限、停止/再開、全体停止/休眠/会話アーカイブ時の待機理由を表示。
- 設定→バックアップ: BackupStatus.jsx、人工履歴と「今すぐバックアップ」。実ファイルは作らない。
- 設定→モデルと接続: SubscriptionConnection.jsx、未接続/接続待ち/中止/接続完了/解除のデモ。外部ログインリンクは作らず、明示的な「デモの接続を完了」操作に置換。
- メンバー→プロフィール: 本体のモデルカタログ操作を抽出し、Codex/Ollamaのサンプル一覧、接続前エラー、モデル/推論設定の選択を追加。
- 設定→モデルと接続→本体のログイン画面を確認: Login.jsxをデモ化。キーdemo-niwaで成功、他の入力はエラー。実資格情報の入力は不要。フォームの入れ子を避けるためPortalとイベント伝播停止を使用。

追加/変更ファイル（prototype/src）: demo-api.js、BackupStatus.jsx、SubscriptionConnection.jsx、Login.jsx、pages/Schedules.jsx、pages/schedules.css（以上新規）、App.jsx、pages/Activity.jsx、pages/Settings.jsx、pages/Members.jsx、styles.css。既存の長文18発言デモ、吹き出し、配色・アイコンを維持。
demo-api.jsはfetchを持たないローカル専用状態。新しい予定・認証状態・モデル変更・バックアップ履歴はページ再読込で初期化。予定時刻に自動起動せず、モデル・アカウント・保存先・外部サービスには接続しない。認証やDBの内部処理はデモへ移植していない。本体の追加機能コードは変更していない（スクロール矢印修正を除く）。

検証: 本体とprototypeの最終ビルド成功。Browser plugin not availableのためPlaywright、127.0.0.1:4173、1366x768/390x844。矢印の上下スクロール・ホイール、予定追加/停止/再開、個別会話の担当制限、全体停止表示、バックアップ履歴更新、接続デモ、ログイン失敗/成功、モデル一覧とOllamaの推論無効化、モバイル予定追加、横はみ出しなしを確認。pageerror/console error/Vite overlayなし、/apiおよび外部への通信なし。API・実ジョブ・実端末は今回未検証。スクリプト/スクリーンショットはリポジトリ外。
# 2026-09-07 固定回数停止の撤去

1仕事24応答・訂正3回・Bot間会話16回での手動再開要求を削除。旧回数停止のみ条件付き自動再開。新規の期限未指定仕事は24時間で失敗しない。明示/既存期限・承認・手動停止・予定予算・実モデル利用上限は維持。古い会話とツール交換は保存したまま入力から省き、履歴読取ツールで再参照。外部結果の重複入力を除去。同じ操作3回で方法の再検討をモデルへ指示（強制停止ではなく、ループ防止の保証ではない）。178テストと検証用backend build成功。稼働反映状況はHANDOFFの最新記録を参照。以下の回数上限に関する記述は過去時点。
